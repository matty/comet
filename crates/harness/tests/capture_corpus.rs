mod capture_corpus {
    mod corpus_frames;
    mod corpus_validation;
    mod observed_fields;
    mod sanitizer_publication;
    mod sanitizer_safety;
    mod sanitizer_semantics;
    mod support;
    mod surface_map;
}
