{
  "name": "comet-native-edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 27640",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run src && node scripts/installer-provenance.test.mjs && node scripts/distribution-config.check.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^6.0.3",
    "vitest": "^3.2.7",
    "wrangler": "^4.111.0"
  }
}
