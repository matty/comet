# Carried-forward debt

Known-open work that is **not** on a phase plan, because it was discovered
mid-slice and deliberately deferred rather than planned. This file is the index:
one row per item, scannable in one pass. An item whose *reasoning* matters gets
its own page beside this one, linked from its row.

Read this before starting a slice — several rows name the slice that owns them.

## How to keep it

- **Add a row when a slice defers something.** The row is the minimum: what it
  is, which review or capture found it, and what state it is in.
- **Write a page when the reasoning outlives the row.** A ruling ("this is a
  decision, not an oversight"), a corrected premise, or a mechanism someone will
  otherwise re-derive. A page is `D<nn>-<slug>.md`, and it is what a PR comment
  or a code comment links to.
- **Delete a row when it merges** — except where the *explanation* still stops
  someone reading the code wrong. Those move to [`closed.md`](closed.md), and
  the row stays with a `☑ merged` state and its PR.
- **Numbers are never reused.** A deleted D-number stays deleted; rows and slice
  logs elsewhere refer to them by number.
- **Deleting a row that code cites now fails a test.**
  `crates/engine/tests/debt_citations.rs` walks every `.rs` file under `crates/`
  and `apps/` and requires each cited `docs/debt/*.md` path to exist and each
  cited `D<nn>` to appear here or in [`closed.md`](closed.md). It exists because
  the 2026-08-29 batch deleted rows out from under live citations three times
  (and because nine files cited a `DEBT.md` that had not existed for months —
  D125). If it fails on a row you are closing, that is the signal to move it to
  `closed.md` rather than delete it.

Legend: ☐ open · ◐ partly closed · ☑ merged

## Open

| # | Item | Origin | State |
| --- | --- | --- | --- |
| [D3](D03-unavailable-agent-selection.md) | ~~An unavailable agent can still be the selected one~~ — **not a defect**. Residual: no *pre-send* signal in a locked session | 0.2a | ☐ low priority, optional polish |
| D13 | Codex declares `auto-accept-edits`, but the linked-worktree sandbox workaround silently raises that mode's workspace-write sandbox to `danger-full-access` — trigger is any linked worktree on a slash-named branch, i.e. this repo's own `feature/…` convention — with only a `tracing::warn!` as the signal | 1.3 whole-branch review | ☐ open — 1.3 documents the exception in the capabilities comment; making it visible to the user needs a notice per `user-facing-errors.md`. **1.5 did not claim it and neither did 1.8** — 1.8 made the mode *selectable*, which makes this reachable by a user rather than only by the default, so it is now **unowned and more visible than when it was filed** |
| D19 | `ApprovalRequest::Mcp` carries **no arguments**, so an MCP approval cannot be identified precisely enough to allow again — 1.6 made it un-allowlistable, so "Allow for this session" silently degrades to a plain Allow on MCP cards. The card is equally blind: `server · tool` and no arguments | 1.6 PR #37 review | ☐ open, **owner is 1.7 or later**. Needs the proto variant to carry a discriminating digest of the arguments, which is a `PROTOCOL_VERSION` question 1.6 froze. Fixing the signature and the card are the same change |
| D20 | Codex's `acceptForSession` **works** and is deliberately unused: 1.7 sends `accept` on both allow arms so Comet's engine stays the single grant authority. The server's grant is keyed on **path alone** and spans the operation kind, while `approval_signature` keys on path **and** operation — delegating would leave two caches with different scopes | 1.7 capture, runs 5–6 | ☐ open **by decision, not by omission**. Revisit only if the two-cache divergence is judged acceptable; it needs a provider-specific carve-out in `engine/src/approvals.rs`, the one function 1.6 built to be provider-agnostic |
| D21 | `cancel` — Codex's "deny **and** interrupt the turn" decision — has no button. 1.5's decision triple maps Deny to `decline`, which lets the agent continue; nothing expresses the interrupting variant except Stop, a separate gesture the user has to know to reach for | 1.7 capture + generated schema | ☐ open, **unowned**. Untested live. **1.9 shipped as the unattended-policy slice and did not touch this** — the "candidate owner is 1.9" note was speculative and is stale |
| D22 | `item/permissions/requestApproval` — a third Codex approval method (filesystem/network permission overlays) that fits no `ApprovalRequest` variant. 1.7 answers it as `Unknown` written **blind**: no capture run produced one | 1.7 capture / `ServerRequest` schema | ☐ open. Capture the first live one and revisit the mapping; until then the card says "Grant Codex additional permissions" and the grant is un-allowlistable |
| D23 | `availableDecisions` arrives on every command approval, is **absent from the generated schema**, and lists only the decisions the CLI's own picker would show (`accept`, `acceptWithExecpolicyAmendment`, `cancel`) — never `decline`, which nonetheless works. Comet ignores the field | 1.7 capture, runs 1/3/4 | ☐ open, low priority. It becomes a signal worth reading if a future CLI starts *enforcing* it, at which point Deny would silently stop working with no error |
| [D25](D25-composer-picker-rework.md) | **The chat/composer surface and the picker menu need a rework, not another feature bolted on.** Every permission, approval and traits control added in Phase 1 landed inside a composer and a merged model+traits popover designed for neither | 1.8 rendered check, user ruling | ☐ open, **accepted deliberately**. Not a blocker for 1.8 or for Phase 1; the next slice that wants to add another control to either surface should stop and cost the rework |
| [D26](D26-done-status-no-expiry-variant.md) | `DoneStatus` has no variant for an unattended expiry — `expire_unattended` ends the turn through the same `interrupt()` a user's Stop calls, so both settle `Interrupted`; only the appended note names the real cause | 1.9 spec §8, deliberate | ☐ open **by decision**. Needs a new `DoneStatus` variant, which is a `PROTOCOL_VERSION` question — bundle it into a bump made for another reason rather than spending one here |
| [D27](D27-sweeper-call-site-uncovered.md) | `Engine::assemble_runtime`'s sweeper spawn (`crates/engine/src/lib.rs:389-393`) is exercised by no test: **deleting it, or swapping `bound` for `sweep_interval(bound)` in it, would leave the whole suite green**. The existing sweeper test calls `spawn_unattended_sweeper` itself and so bypasses the call site | 1.9 Task 5 review, controller ruling: do not reopen | ◐ **transposition half closed 2026-08-30 by the type fix the page itself recommended**: `UnattendedBound(Duration)` is taken by `spawn_unattended_sweeper` and by `sweep_interval`, which still returns a plain `Duration` — so the cadence cannot travel back into a bound position. Verified by writing the swap and watching the build fail, not by a timing assertion; the module test pins the CLAMP that made the mistake invisible instead (`sweep_interval(100ms) == 250ms`, LARGER than its input). **The deletion half stands open with its ruling intact** — nothing type-checks a call that is not there, and reaching it still needs `assemble_runtime` to take an injected registry, which is the cost 1.9's review declined |
| [D29](D29-administrative-clients-count-as-supervisors.md) | **An administrative CLI call counts as a supervisor.** `comet status` and the `comet remote …` subcommands open the same IPC websocket the Desktop app does, so each one clears `unattended_since` and stamps a fresh stretch on exit — a cron polling `comet status` every five minutes means a parked approval **never expires**, with no log line saying why | 1.9 whole-branch review | ☐ open **by decision**. Fails in the safe direction; distinguishing a supervising viewport from a one-shot administrative call needs a hello-handshake field and a `PROTOCOL_VERSION` bump |
| D30 | **A forced discovery retry can swallow another caller's drift signal.** `DiscoveryCache` reports an unreadable answer once per attempt, and `clear()` starts a fresh attempt — so a forced retry landing between another caller's `get()` and its report swaps the attempt out, and that caller finds nothing to report. One `Unparseable` then raises no `Diagnostic` | 2.1 PR review (#41) | ☐ open **by decision**. Only loses the signal when the retry it raced *succeeded*, so the drift is over by then. Closing it means returning the failure alongside the catalog from `Harness::models()`, changing the trait's return type and every fixture implementing it, for a window that needs a concurrent forced retry. Documented on `take_unreported_failure` |
| D31 | The initialize handshake returns `agents` on the same reply comet already reads for models, and it is deliberately dropped. **After 4.2 the subagent *events* exist** (`SubagentStarted`/`SubagentUpdated`, tasks 1–7) and the list is still unconsumed, for a different reason: `agentType` arrives per-run on `task_started` — `agent.rs:1425-1427`'s own comment says it is "never looked up in the discovery handshake's `agents` catalogue" — so nothing needs the catalogue, and the entries' `description` fields are long model-facing prose (the `Explore` entry measures **605 characters** of instructions in the 2026-08-11 initialize capture), which is not card copy | 2.2, deliberate (spec departure 1); reason corrected by 4.2 | ☐ open **by decision** — free data, still no consumer, and no later slice now owns closing it (4.2 was the named owner and reconfirmed the row rather than closing it). Wire shape is in the 2026-08-11 initialize capture |
| D54 | **The child subagent's own transcript is not reachable from the parent's card** — only its activity line, summary and totals. `AgentEvent::SubagentStarted`'s `tool_use_id` is kept for exactly this and matched nowhere as a key today | 4.2, deliberate | ☐ open — **still open after 4.4, and by ruling rather than omission.** The row read "4.4's spec is unwritten and nothing claims it yet" until 2026-08-30; 4.4 shipped on 2026-08-26 ([PR #115](https://github.com/matty/comet/pull/115)) and ruled **cards, not a tab**, because a subagent's inner transcript is dropped at the normalizer and never persisted — a tab would have had nothing to show. Reaching it means persisting what the normalizer drops, which is its own slice and unclaimed. `crates/doc/src/parts.rs:358-359`'s comment: "`tool_use_id` is kept on the event for the journal and a later slice's child-transcript render, never as a match key here" |
| D55 | **`workflow_progress` — a coordinator's `phases[]`/`agents[]` array on `task_progress` — is never decoded.** A single `Agent` call never produces it, so 4.2's capture has none. **Not decoding it is what keeps one coordinator tick at one `SubagentUpdated`** — t3code fans the array out into per-member events, which is where the up-to-100-events-per-tick risk lives; Comet never reading the array is what avoids it, not the dedupe filter. The material-transition filter (`SubagentSnapshot`, `normalize.rs:141-158`, reached via `emit_subagent_update` at `:479-506`) is an orthogonal mechanism Task 3 built to collapse repeated *identical* `task_progress` ticks — it would do nothing against a real fan-out of distinct per-member frames | 4.2 spec risk, decided in Task 3 | ☐ open **by decision**, deferred at spec time rather than found mid-slice. The first user to run a `Workflow` produces the first real frame; there is still no capture of one. **If a future slice decodes the array, it must build its own fan-out guard — the existing filter does not provide one** |
| D56 | **`render_parts`'s catch-all passes a `Subagent` part's `summary` and `description` through unfiltered, and `summary` is the child's full answer, uncapped end-to-end.** Its own `Approval` arm comment (`crates/engine/src/sessions.rs:1346-1351`) says a kind carrying something heavy "has to make that decision here rather than inherit it silently" — `Subagent` inherits it silently. `prompt` genuinely has nothing to strip: the persisted struct (`crates/doc/src/parts.rs:95-118`) has no `prompt` field at all. `summary` is different — `normalize.rs:574` assigns it straight from the wire (`summary: f.summary`) with no `cap_prose`, unlike `prompt` (`:532-534`) and `Notice` text (`:368`, `:386`); the fold (`doc/src/parts.rs:458`) adds no cap either, so the child's unbounded answer reaches the persisted doc and every LAN peer's copy | 4.2 review; `summary`'s cap decided in Task 3's own review round (Finding 3) | ☐ open, low priority. **The `summary` half is a decision, not a gap.** Task 3's review round weighed capping it against `TextDelta` and `Done.result` — also uncapped anywhere in `normalize.rs`, also the product's own displayed content — and concluded a subagent's answer is that same kind of content, not `Notice`/`prompt`'s kind (deliberately kept out of what's shown), so capping it here would truncate real content before any surface decides how to present it. `description` crosses the same catch-all with no dedicated cap either, unreviewed — it has only ever been observed as short task-title text, never reasoned about the way `summary` was. What is missing is that `render_parts`'s own site — the one whose comment demands the decision be made there — records none of this |
| [D57](D57-steer-leaves-a-subagent-running-forever.md) | **Task 7's "no `Subagent` left `Running`" guarantee is not total.** A steer finishes the pre-steer segment `Complete` (`sessions.rs:1776`) and deliberately leaves a still-`Running` `Subagent` part untouched (`cancel_running_subagents`'s doc, `sessions.rs:1440-1467`); `DocHost::mark_abandoned_streams` sweeps only `Streaming` entries (`doc_host.rs:163`), so the crash-path sweep task 7 added doesn't reach a finished segment either — a subagent still running when the user steers stays `Running` forever in that segment | 4.2, deliberate (user ruling) | ☐ open **by decision**, documented at the source and on the linked page. `sessions.rs:1440-1467` is the record: Comet sends nothing that would abort a subagent still running at the steer boundary, and what the CLI does with it from there is uncaptured, so stamping `Cancelled` there would assert an outcome nobody observed — a stale `Running` reading beats a manufactured one. Proven end-to-end by `crates/engine/tests/e2e.rs:2625-2686`. **4.4 gave the state a face and left the row open:** the card reads `last seen running` with "You sent a new message before this finished." — reporting where it froze, never an outcome — detected from the part being `Running` inside a `Complete`/`Aborted` entry, so no new `SubagentStatus` variant and nothing reaching `PROTOCOL_VERSION`. **Not reachable from the mock harness**, which advertises `supports_steering: true` and never drains `controls.steering`; only the Claude and Codex adapters emit `AgentEvent::Steered`. Seeing it needs a real Claude run steered mid-flight |
| D32 | The slash-command cache the phase spec puts in 2.2 is not built: `commands` is **cwd-scoped** (66 entries in this repo vs 63 in a home directory, the delta being its project skills) while `Harness::models(&self)` has no cwd to key on | 2.2 capture, 2026-08-11 | ☐ open **by decision** — moved to **2.4**, which owns the `/` menu and has a cwd in hand. Reading it here would cache another directory's skills. **2.4 also cannot reuse the discovery spawn**: it passes `--bare`, which skips user and project skill discovery (42 commands vs 66), so the command list needs a spawn of its own |
| D33 | `claude-opus-4-8` and `claude-opus-4-7` are curated but absent from Claude's live model list, so nothing live confirms them and no test would notice if they were retired | 2.2 capture, 2026-08-11 | ☐ open, low priority. **They still work — they are simply not in the official list** (user, 2026-08-11), and the handshake cannot tell you: the CLI does not validate `--model` at session init, where a bogus id also answered and exited 0. Merge rule 1 (discovery never deletes a curated model) is what keeps them, and a retired model fails loudly at run time |
| D35 | **Codex reports per-model deprecation copy and Comet drops it.** `model/list` carries `upgrade` (a replacement model id) and `upgradeInfo.migrationMarkdown` ("GPT-5.4 will be deprecated soon… Switch to GPT-5.6 Terra to continue.") on `gpt-5.4` and `gpt-5.4-mini`; 2.3 models neither | 2.3 capture, 2026-08-11 | ☐ open, low priority. Phase 0b's "neither provider emits a deprecation notice" was about the **event stream** and is still true — this is a field on a catalog reply. A notice surface already exists (`AgentEvent::Notice`, 0b.1); the open question is whether a catalog reply is a legitimate producer for it |
| D36 | **`defaultReasoningEffort` has nowhere to go.** Codex reports a different default effort per model (`sol` low, `terra`/`luna` medium, `codex-spark` high) and `comet_proto::Model` carries a ladder with no declared default, so the picker cannot show or honour it | 2.3 capture, 2026-08-11 | ☐ open. Adding it is a `Model` field, so it costs a `PROTOCOL_VERSION` question — read that constant's doc comment before deciding whether an absent value is safe for an older peer |
| D37 | **Two models are offered a service tier the server does not list for them.** `gpt-5.4-mini` and `gpt-5.3-codex-spark` come back with `serviceTiers: []`, while `codex/catalog.rs` gives all seven curated models the same `serviceTier` option — and merge rule 2 (curated capability wins on a matched id) preserves that by design | 2.3 capture, 2026-08-11 | ☐ open, low priority. Pre-existing; 2.3 deliberately did not touch the merge rule to fix it. Closing it means letting a live answer *remove* a curated option, which is the opposite of rule 2 and needs a ruling, not a patch |
| D38 | **A logged-out `codex` answers `model/list` successfully with a five-model list baked into the binary** — it contains `gpt-5.2`, which the account cannot use, and omits three models it has. Comet's defence is a pre-spawn `auth.json` check, which cannot see a user authenticated purely by an `OPENAI_API_KEY` environment variable | 2.3 capture run 6, 2026-08-11 | ☐ open **by decision**. Fails safe: an env-only user gets the curated list under the "couldn't reach Codex" caption, never a list belonging to nobody. Closing it properly means asking the server who is logged in (`account/*`), which needs its own capture |
| [D39](D39-codex-skill-input-item.md) | **Codex has a real command surface and 2.4 does not use it.** Its app-server does not parse slash commands at all — `/name` reaches the model as literal text and spends a turn on a refusal — but `skills/list` answers in 0.36s with no thread, and `turn/start` accepts a structured `{type:"skill", name, path}` input item, verified working | 2.4 capture, 2026-08-11 | ☐ open **by decision** (user ruling, 2026-08-11). Needs a structured reference to survive composer draft → proto → engine → harness, which is a wire design with a second consumer: Codex's `{type:"mention"}` item for `@` files, which Comet also does not use. Its own slice |
| D40 | **The `/` menu's spawn runs the user's `SessionStart` hooks.** It is the non-bare handshake by necessity (`--bare` drops user and project skills, 42 against 67), so every first `/` in a new directory runs whatever the user has configured — 3.5s of hook in the 2.2 measurement, and arbitrary side effects in principle | 2.4, deliberate | ☐ open **by decision**. Bounded by the per-directory per-boot cache, so it is once per directory, not once per keystroke. Closing it properly needs a CLI flag that skips hooks without skipping skills, which does not exist today |
| D50 | **A second install of the same CLI is invisible, and PATH order alone decides which one runs.** The capture machine has Codex twice — native `0.147.0` under `%LOCALAPPDATA%\Programs\OpenAI\Codex\bin` and npm-global `0.146.0` under `%APPDATA%\npm` — and `npm ls -g` confirms the npm one. `resolve_cli` (`harness/src/lib.rs:171-200`) flat-maps *directories* first and filename spellings second, so directory order dominates and `executable_names`' `.exe`-before-`.cmd` rule only breaks ties **inside one directory**. Reorder PATH and Comet silently runs the older CLI | 3.1 capture, 2026-08-11 (`captures/2026-08-11-agent-version-install-method.md`) | ☐ open **by decision** (user ruling, 2026-08-11). 3.1 shows the resolved install only. Detecting a shadowed one means continuing the directory scan past the first hit and spawning `--version` per extra candidate at every boot, plus a new user-facing state with its own copy and failure story. The version+path line 3.1 adds is what makes the situation *diagnosable* today: the path names which install answered. Revisit with 3.2 — `claude install` is the native updater and cannot update an npm-global install, so an Update button has to know |
| D41 | **The command cache never refreshes inside a boot.** A skill added to the repo while Comet is open is invisible to the `/` menu until restart, and an unrecognized command fails *silently-successfully* — the CLI answers `Unknown command: /x` as ordinary assistant text with `is_error: false` and no model call, so nothing downstream can detect the staleness either | 2.4, deliberate | ☐ open. The picker's Retry already clears it (`clear_discovery` clears both caches); what is missing is a trigger the user would think to reach for. Codex publishes a `skills/changed` notification for exactly this, which is a reason to solve it alongside D39 rather than alone |
| [D43](D43-fake-provider-launch-contract.md) | **The fake providers do not prove the full process launch contract.** Claude ignores every run argument except permission mode; Codex never checks the `app-server` subcommand; resume, attachment and actual child-cwd paths remain partly or wholly outside subprocess coverage | fake-provider review, 2026-08-11 | ☐ open, highest-leverage first hardening step. D34 separately owns relative executable resolution |
| [D44](D44-provider-capture-corpus.md) | **The provider capture corpus is partial.** Every retained capture-derived assertion rests on sanitized, versioned literal evidence, including the successful Codex steer request/reply/completion sequence. The all-twelve-scenarios criterion remains unmet: ordinary approval, on-request approval, and interruption produced only quarantined contradictions | fake-provider review, 2026-08-11; bounded capture work, 2026-08-12 | ◐ partly closed. The three ignored partials cannot back assertions; checking incomplete evidence into the corpus requires a separate contradiction-artifact design |
| [D45](D45-provider-lifecycle-fault-matrix.md) | **The provider fakes cannot express most lifecycle faults.** There is no setup-stage hang, crash-plus-stderr, partial frame, broken stdin, late reply, or provider tool call that never completes | fake-provider review, 2026-08-11 | ☐ open, preferred reliability slice. D5 covers the user-visible hung-call state, not a provider-fake reproducer |
| [D47](D47-live-provider-canaries.md) | **No automated live turn checks the scripted run protocols for drift.** The ignored real-CLI tests cover token-free discovery only; approvals, steering, notices, interruption and terminal mapping rely entirely on fakes and historical captures | fake-provider review, 2026-08-11 | ☐ open by environment constraint. Candidate for an opt-in or scheduled authenticated canary, never the ordinary PR gate |
| [D48](D48-provider-state-sequences.md) | **Named scenarios cover examples, not the event-order state space.** JSON-RPC response/notification races and stream lifecycle permutations can regress outside the hand-written transcripts | fake-provider review, 2026-08-11 | ☐ open, after D45 supplies reusable fault primitives. D8–D10 already own the known diagnostic-policy gaps |
| [D49](D49-provider-cross-boundary-tests.md) | **The provider subprocess fakes stop at the harness boundary.** Engine tests use in-process harness doubles, so selected launch, approval, discovery and terminal-state paths never cross fake executable → harness → engine → RPC in one test | fake-provider review, 2026-08-11 | ☐ open, targeted integration coverage rather than a duplicate full suite |
| D58 | ~~`comet-provider-sanitize` has no rule for a subagent `task_id`, in any context, and none for `task_notification`'s `output_file` field at all... none of which get redacted.~~ Named a blocklist-era gap: `task_id` matched none of the old `semantic_kind`'s recognized key spellings — that function is gone, replaced by `allowlist.rs`'s `named_kind`/`NAMED_LEAVES` — and `output_file`'s embedded cwd/session/task_id slug had no value-based sweep to catch it. **The leak is closed**: neither `.task_id` nor `.output_file` is on `claude.txt`, so both now default to redacted, and `sanitize_scalar` replaces the *whole* string, not just a recognized substring within it — on the re-sanitized `subagent` capture every `task_id` occurrence reads the same `<V245>` (value-based reuse already gives it the "joins survive" property a named kind exists for) and `output_file` reads `<V258>`, with nothing of the mangled slug surviving inside it. The pinning test this row cited, `sanitizer_has_no_rule_for_task_id_or_output_file_mangled_cwd_slug_known_gap`, lived in `sanitizer_semantics.rs`, deleted with the other thirty example-based tests it was one of and superseded by `allowlist_property.rs`'s total property, which now covers this case among all others | found hand-sanitizing `claude/2.1.229/subagent` for 4.2 task 9; general mechanism closed by the allowlist-sanitizer stage, 2026-08-15 | ◐ partly closed. The leak is gone; what is left does not merit its own row. `task_id` still isn't one of `NAMED_LEAVES`' six kinds, so it reads as a bare `<V245>` instead of a readable `TASK_`-prefixed name — a review-readability gap, not a safety one, since the same placeholder already appears at every occurrence |
| [D63](D63-exploratory-capture-has-no-route.md) | **The corpus pipeline has no route for an exploratory capture, so the two captures that needed one both went around it.** `docs/testing/provider-captures.md` requires every corpus entry to come from a named `comet-provider-capture` scenario through `comet-provider-sanitize`, and forbids copying raw data in or hand-editing staged output. But a scenario can only be written once you know what you are looking for, which is the thing an exploratory capture is for. 4.2 resolved this by hand-sanitizing a `drive_claude_plan.py` capture and saying so in the manifest's `source` field; 4.3 resolved it by adding two scenarios after the fact and re-capturing | 4.2 task 9, and again in 4.3 | ☐ open. Not a bug in either tool — the rig and the corpus are both doing their jobs. The gap is that the doc describes only one of them, so each slice re-decides the bridge on its own |
| [D65](D65-plan-mode-needs-its-own-channel.md) | **Plan mode is not a fifth `RuntimeMode`, and making it one would be wrong.** `--permission-mode plan` is a real CLI mode Comet's four-mode mapping omits, but it needs three things this slice does not touch: `requires_user_interaction` decoded off `can_use_tool`, `ExitPlanMode` intercepted by tool name and routed somewhere that is not an approval card, and `AskUserQuestion` given a real answer channel — the capture rig replied `{"behavior":"allow"}` to one and got back the tool_result *"The user did not answer the questions."*, so approve/deny is structurally the wrong control for both | 4.3, deliberate (capture §3) | ☐ open, unowned. Its own slice. The parent design's second axis (`ProviderInteractionMode = default \| plan`) already anticipated it, and t3code implements it orthogonally to the permission axis — evidence for the two-axis split, not against |
| D66 | **Task dependencies are not decoded.** `TaskCreate`/`TaskUpdate` accept `addBlockedBy` / `removeBlockedBy`, and t3code reads them at `ClaudeAdapter.ts:877` and renders `"subject (blocked by #2)"`. No capture has ever produced one | 4.3, deliberate | ☐ open. Left undecoded rather than blind-coded: `.agents/rules/optional-wire-fields.md` cuts both ways, and inventing the PRESENT case from typings has the same failure mode as assuming the absent one never happens. `ChecklistItem` would need a field before this can land |
| D67 | **An item leaving a Claude task list has never been observed, so nothing handles it.** `TaskCreate` and `TaskUpdate` are the only mutations any capture has recorded; there is no delete, and a `TaskList` snapshot is the only route by which the accumulated list could shrink on the Claude side. Codex has no such gap — its plan arrives as a whole snapshot every time | 4.3 | ☐ open, low. If a delete exists, a per-run checklist would show a step that is no longer in the plan until the next `TaskList`. Bounded by the run |
| [D68](D68-checklist-is-scoped-per-run.md) | **A resumed run's checklist is partial, by construction.** `MessagePart::Checklist` is scoped to the RUN — native for Codex, whose plan is turn-scoped, and an imposition on Claude, whose list is session-scoped and outlives the process. A resumed process restates nothing, so its card holds only the items that run touched: two of three in the recorded pair, with the item completed earlier invisible | 4.3, deliberate | ☐ open, and **this is the row 4.4 must read before designing the card**. The fix, if 4.4 judges it needed, is carrying the list across runs as engine state plus a migration — its own slice, not a tweak. **4.4 drew the card and did NOT answer the question.** It renders what the part holds and synthesizes nothing: a step the run never saw the subject of falls back to `activeForm`, then to a muted "Unnamed step" rather than a blank line (`a_step_label_falls_back_through_active_form_to_a_placeholder`). Whether a *short* resumed card reads as a bug is still unanswered, because the mock cannot pose it — a second message re-runs the same script rather than resuming a server-held list, so both cards hold the same steps. It needs a real multi-run Claude chat |
| D69 | ~~**The supported-provider-version floor is prose, and nothing enforces it.**~~ | 4.3, from the user's minimum-supported-version ruling | ◐ **the installed-version clause is closed.** The background probe blocks only a strictly dotted-numeric version below the documented Claude Code, codex-cli, or Grok floor, with provider-specific update/install copy; equal, newer, missing, prerelease and malformed versions stay selectable. Its behavioral test reads the same committed floor table, while `version_floor.rs` ties every row to a promoted corpus directory in both directions. `HarnessInstall` and `HarnessUpdate` survive the unavailable result. **Residual, capture-blocked:** Codex tool NAMES still have no coverage lint because no promoted scenario exercises a Codex tool call, so there is no discriminator path to declare. The other coverable surfaces are checked: ACP `sessionUpdate` kinds, Codex methods (`codex_method_coverage.rs`, D90), and Claude tool names (`claude_tool_coverage.rs`). |
| D70 | **The corpus has no version-coverage policy and no link to a Comet release.** `claude/2.1.228/` and `claude/2.1.229/` coexist with no rule about which versions must be covered, when an old one may be retired, or which provider versions a given Comet build was actually tested against. A user reporting a bug on some CLI version cannot be answered from the repo | 4.3 | ◐ **coverage half closed 2026-08-30 ([PR #194](https://github.com/matty/comet/pull/194)).** `docs/testing/supported-provider-versions.md`'s table gained a **Latest promoted** column per provider, checked against the corpus on disk by `crates/capture/tests/capture_corpus/coverage_policy.rs` — a promotion that does not update it now fails the build. It was written against real drift, not a hypothetical: `claude/2.1.251/` was promoted ([PR #190](https://github.com/matty/comet/pull/190)) and **nothing in the repository named it for four commits**, because nothing compared the document to the corpus. **The release link is the commit** — nightly tags carry `source_sha` and the table lives in that same history, so `git show <source_sha>:docs/testing/supported-provider-versions.md` answers what a build was tested against, with no separate changelog to drift. **Retirement stays prose and is NOT enforced**, deliberately: judging whether a decode's *only* evidence is unique to a departing version is the per-version form of D69's open question, and the three decode-coverage lints show that evidence exists, never which version supplied it. The rule is in `provider-captures.md`'s "Corpus coverage and retirement". **Remaining:** an actual retirement; the installed-floor half is now D69's closed clause |
| D71 | **Live provider e2e tests are unreliable instruments, in two distinct ways.** (1) **Discretionary behaviour:** Codex publishes a plan when it decides the work is multi-step, so two identical runs on 2026-08-14 gave a full three-step plan and then nothing in four minutes — a live plan test written for slice 4.3 was removed rather than shipped as a coin flip. (2) **Unanswered approvals park to the timeout:** the same run escalated to an approval after its sandboxed read failed, and an `#[ignore]` test that answers none simply hangs until its deadline with no signal about why | 4.3 task 9 | ◐ **(2) closed 2026-08-30 ([PR #189](https://github.com/matty/comet/pull/189)); (1) untouched and still open.** **(2)'s premise was wrong and the correction is the durable part:** the live tests never parked on an approval at all — `Box::new(\|_\| oneshot::channel().1)` drops the sender in the same statement, so every harness mapped it to `Expired` and declined instantly. The hang came after, when a run stalled for an unrelated reason and the outer `timeout(..).expect("the turn settles rather than hanging")` fired having already discarded the fact that an approval was ever raised. `support::recording_decliner` keeps that record and `support::settle_or_report` fails the moment it is written, naming the approval. Fail-fast was chosen over a default responder deliberately: a test that grants itself filesystem or command access answers on nobody's authority and would hide the escalation this row wants visible. **Consequence, intended:** those runs now fail on *any* approval, not only one that leads to a hang — right for these tests, which are documented as deliberately trivial and tool-free, so an approval at all means something unexpected happened. Proven by `approval_diagnostic.rs`, which drives the helper with an in-process double and is **not** `#[ignore]`d, so the helper's own behaviour is gated by CI even though the live tests are not. **(1) may still have no fix** beyond asserting on what a provider volunteers rather than what it should do |
| [D74](D74-redacted-empty-strings-are-unobservable.md) | **A redacted field's empty case can never again be demonstrated from corpus evidence.** The sanitizer redacts every `String` scalar on an excluded path the same way regardless of whether the original value was empty — a placeholder is never itself empty — so once a path is excluded, "the provider sends an empty string here" is unrecoverable from the archive. Found when re-sanitizing broke `commands.rs`'s `an_empty_argument_hint_is_absent_not_blank`: `argumentHint: ""` (the CLI's real "no arguments" signal) and a populated hint both now read as "present" | Task 5, allowlist-sanitizer stage, 2026-08-15 | ☐ open, **not fixable in general**. The test was moved to a hand-written fixture testing `non_empty()` itself rather than corpus evidence, which proves the decoder but no longer proves the provider still sends the empty case. Watch for the same shape (absence-vs-emptiness is a decoded distinction) on any future path excluded from an allowlist |
| D76 | **One re-sanitize pass collapsed distinct token-counter values to a shared placeholder, confined to `claude/2.1.229/subagent`.** Dropping `.tool_use_result.pin.ref` from the allowlist required re-sanitizing that one capture by reconstructing `capture.json` from its own already-sanitized `manifest.json`/`events.jsonl` and feeding it back through `sanitize_dir`. The reconstruction patched 172 already-redacted numeric-counter fields (`input_tokens`, `output_tokens`, `cache_creation_input_tokens`, `cache_read_input_tokens`, `total_tokens` — unlisted, so already placeholders, not raw numbers) to a shared dummy `0` rather than 172 distinct dummies, so the redactor's own "equal values share a placeholder" rule now merges positions that held genuinely different original numbers. `AGENTS.md` says "equal values share a number so joins across frames still work" — for these positions in this one file, that inference is now false | Final review, allowlist-sanitizer stage, 2026-08-15 | ☐ open, **low severity, narrow scope** — confined to the five counter-field families above, only in `claude/2.1.229/subagent`, and the fields were already unlisted placeholders with no real value to leak either way. Fix at the next promotion of this scenario: map each distinct pre-existing placeholder to its own distinct dummy integer (not one shared `0`) before reconstructing, which preserves the equivalence classes exactly |
| D77 | **A map whose path nobody declared still publishes its keys.** The key rule added in this stage is driven off `surface::MAP_PATHS`, a hand-declared list holding one entry (`.modelUsage`). A provider that starts keying an object by something identifying — an MCP server name, an account, a machine — at an *undeclared* path has its keys published verbatim into the committed archive, because an undeclared object's keys are treated as field names and field names are published on purpose (each version's capability sheet under `docs/providers/` is a snapshot of exactly those). Proven blind, not assumed: a rogue key planted under `.modelUsage` with no scalars beneath it fails `every_committed_map_key_is_allowlisted_or_a_placeholder` while `every_committed_value_is_allowlisted_or_a_placeholder`, `every_manifest_token_is_declared_or_a_known_path_root` and the capability sheet's golden test all stay green | Finding 1, PR #66 review, 2026-08-15 | ☐ open, **gated but not closed**. The real backstop is the capability sheet: an undeclared map shows up in its Fields section as a field with an obviously data-shaped name, which fails `every_corpus_version_matches_its_committed_sheet` at promotion (`crates/capture/tests/capture_corpus/capability_sheets.rs`, stage 5's successor to the deleted `observed_fields.rs` gate) — `surface.rs`'s `MAP_PATHS` comment already documents that triage step, and it is the one that adds the declaration. So the residual is "a human must read a failure correctly," not "it lands silently." Closing it properly means inferring maps by shape, which `surface.rs` rejected for a good reason (a one-model machine's key set looks perfectly stable), so the cheaper honest close may be a heuristic *warning* on any object whose keys don't look like identifiers rather than a rule. **First real instance, 2026-08-29** (`feature/sanitize-vendor-dotted-keys`): `.params.update.usage.modelUsage` was undeclared while `.result._meta.usage.modelUsage` was declared — Grok sends the same model-keyed map on the `turn_completed` notification as well as the prompt reply. Now declared. It narrows the row's own claim about the backstop: **the sheet cannot catch this for a provider with no promoted corpus**, which is every provider at the moment the first capture is being reviewed, so the declaration came from reading the raw capture's keys directly. A promoted provider keeps the sheet backstop; a new one has nothing but the reader. **Second live instance, 2026-08-29** (found reviewing [PR #142](https://github.com/matty/comet/pull/142), not fixed there): `.params.toolCall.rawInput` on ACP's `session/request_permission` is the same tool-argument map as the declared `.params.update.rawInput`, and is undeclared — `acp/approval.rs:144,150` decodes `command`/`cwd` off it. Declared 2026-08-30 ([PR #180](https://github.com/matty/comet/pull/180)) in `surface::MAP_PATHS` with `command` and `cwd` as `named_children`, mirroring `.params.update.rawInput`; no allowlist line was added, so both values stay default-deny. **No capability sheet changed**, which is the row's own point restated: nothing had published only because no ACP approval scenario is promoted, so the declaration is latent until one is. Pinned by `a_named_map_child_reaches_the_sheet_for_tool_call_raw_input_too` |
| [D81](D81-displaced-runs-escape-ownership-census.md) | **A run displaced after bounded interruption falls out of the ownership census but can later write under a reused chat id.** Replacement registration drops the displaced `RunHandle` without joining the old `drive_run`; after the visible replacement settles, final purge can certify same-id reuse while that hidden task remains able to publish chat-id-keyed journal, status, sidecar, activity, or harness-session writes | Task 9 final-fix round 3 whole-matrix review | ☐ open, **unowned and pre-existing**. Deliberately outside Task 9's bounded repair; it does not invalidate the current delta's bounded PASS. A future lifecycle slice must either retain every displaced task in the ownership census or require a current generation/run proof for every late effect |
| [D82](D82-detached-chat-id-work-lacks-generation-binding.md) | **Detached chat-id work has no exact generation binding, so auto-titling or an in-flight command drain can act on a recreated chat.** Auto-titling carries only `chat_id`; a delayed old drain later calls ordinary dispatch by that id after its one-time host check | Task 9 final-fix round 3 whole-matrix review | ☐ open, **unowned and pre-existing**. Deliberately outside Task 9's bounded repair; it does not invalidate the current delta's bounded PASS. Bind detached work to a current document/handle generation before provider start or durable mutation |
| [D83](D83-history-diff-syntax-highlighting.md) | **History-diff syntax highlighting has no local surface to attach to.** Upstream `d903bd7` assumes a pinned revision and history diff model; Comet currently has only the working-tree Changes surface | tree-sitter upstream sync, 2026-08-16 | ☐ open, deliberately deferred until a history graph/pinned-commit diff surface exists; then port `d903bd7` through `scripts/sync-upstream.py` |
| D90 | **22 method strings named by `crates/harness/src/codex/normalize.rs` appear in no capture anywhere** — measured 2026-08-16 against both the fresh stage-6 captures and the entire committed corpus: `account/updated`, `command/exec/outputDelta`, `hook/completed`, `hook/started`, `item/autoApprovalReview/completed`, `item/autoApprovalReview/started`, `item/commandExecution/outputDelta`, `item/contextCompaction`, `item/fileChange/outputDelta`, `item/fileChange/patchUpdated`, `item/mcpToolCall/progress`, `item/plan/delta`, `item/untyped`, `mcpServer/oauthLogin/completed`, `model/rerouted`, `process/exited`, `process/outputDelta`, `skills/changed`, `thread/compacted`, `thread/environment/connected`, `thread/environment/disconnected`, `thread/name/updated`. Each is a decode path that ships never having been constructed — the failure `.agents/rules/optional-wire-fields.md` exists to prevent | stage-6 corpus audit, 2026-08-16 | ◐ **the measurement is automated and it corrected the list, in both directions.** `crates/capture/tests/capture_corpus/codex_method_coverage.rs` re-measures on every run, so the count can no longer drift the way a 2026-08-16 afternoon's list can. **The 22 above are 25**, and the difference is not drift: `model/rerouted` and `item/contextCompaction` are named ONLY inside `#[cfg(test)]` blocks, so no shipped decode mentions them and they were overcounted; `item/permissions/requestApproval` (D22's own row), `item/reasoning/textDelta`, `item/untyped`, `turn/aborted`, `turn/failed` and `turn/plan/updated` are dispatched on in production and were missed. **The triage this row asked for is the split**: 17 are entries in `IGNORED_NOTIFICATIONS` with no capture behind them, which cost a dead row and reach no decode; **8 are CLAIMED** — real decode paths that ship never having been constructed, and the number worth watching. Both counts are pinned so a dead row turning into a claimed decode fails even though the total is unchanged. The reverse direction is checked too: one captured method (`thread/goal/cleared`) is named nowhere in production, which is a decision `IGNORED_NOTIFICATIONS`' own doc comment already took. What is left is the same lint for tool NAMES rather than methods, which no Codex capture can support — nothing in the corpus exercises a Codex tool call at all |
| D92 | **The `comet-engine` lib test binary hangs intermittently under `cargo test --workspace`, and its lock holder stalls every later cargo command in the repo.** Observed 3 times in 6 full runs on 2026-08-24/25: the binary sits at **zero CPU across 45s+**, always after exactly 7 test binaries have reported, and the run never completes. Isolated, `cargo test -p comet-engine --lib` passed **5/5 in ~4s each**, so it needs workspace parallelism to reproduce. The expensive part is the aftermath: the abandoned process keeps a build lock, so the next `cargo` invocation blocks with no output that names the cause (`Blocking waiting for file lock` is easy to filter away), and it has twice been mistaken for a slow build | observed while verifying PR #93, 2026-08-24/25 | ☐ open, **not diagnosed**. No specific test is identified — nothing distinguishes the hung binary beyond its name, and the 7-binary stall point is consistent but unexplained. Recorded per AGENTS.md's rule that a flake written off must name where it is recorded; this row is that record, and the count is 3/6 rather than an assertion that it is 'known'. **Check for a stray `cargo.exe` before diagnosing any slow build** — one from a previous session survived overnight and stalled a full day's runs |
| D93 | **Only the watchdog half of upstream `8eabaeae` landed; the parked self-continuation half did not.** PR #93 ported the turn-quiesce watchdog. The same upstream commit also resumes a parked session when the agent self-continues — Claude Code re-invokes itself when a background task finishes, and the parked gate drops that streamed output on the floor, losing real transcript content. That half needs a `seen_tools` set this fork does not have, and was written against ACP re-invocation behaviour we do not run | PR #93, 2026-08-25 | ☐ open **by decision**. The split is safe in this direction only: the watchdog alone is complete, but the resume without the watchdog would not be — it is the watchdog that makes re-arming Working recoverable when no Done follows. Reopen if transcript content is reported missing after a background task completes, which is the symptom that half fixes. The upstream commit is marked `adapted` in the sync ledger, so it will not resurface on its own; this row is the record that part of it is outstanding |
| D94 | **Recovering a zero-byte `device-id` leaves one narrow race unclosed.** `load_or_create_device_id` displaces an empty file by rename, then republishes through create-if-absent, so two processes repairing together normally converge — one wins the `hard_link`, the loser re-reads it (`concurrent_recovery_agrees_on_one_id` covers this, and on Windows the repair also has to survive `ERROR_ACCESS_DENIED` — `rename` fails while another handle is open, so a failed displace backs off and re-reads rather than failing startup; the first version of the fix did not, and 8 concurrent openers caught it). The uncovered interleaving is narrower: process B reads the file as empty, process A publishes a good id, and only THEN does B rename it aside; B mints a second id and A has already returned the first. Both engines then author the same doc under different ids | PR #96, 2026-08-25 | ☐ open **by decision**. Upstream closes this with a real file lock — `flock` on Unix plus a Windows share-mode open with retries. That is platform-specific code on the platform this repository ships and never tests in CI (see "Windows ships, and no PR ever tests it"), traded against a window that needs two engines cold-starting within microseconds of each other on an installation whose identity is ALREADY corrupt. The consequence is split doc attribution, not data loss, and the bug being fixed is a permanently unstartable engine. Reopen if attribution splits are ever observed in the field, or if a lock primitive lands that both platforms already exercise |
| [D91](D91-captures-inherit-the-capturer-s-claude-config.md) | **A Claude capture inherits the capturer's own `~/.claude`, and the archive cannot tell that it did.** The recorder has `--codex-home` / `needs_empty_codex_home` for Codex and no Claude equivalent, so `claude fresh-text` on 2.1.233 from a disposable cwd recorded **62 tools, 37 skills, 68 slash commands, 3 plugins, 2 MCP servers** and a `SessionStart` hook firing mid-capture. The values are redacted; **the `tools` count is not**, and that count is precisely what D86 added to the sheet so roster change would be visible. Rendered against `claude-2.1.228.md`'s `tools: 29`, the version-change report would show **+33 tools** and read as the CLI growing when most of it is one machine's plugin roster | dry-run promotion of a 2.1.233 capture, 2026-08-17 | ◐ partly closed by the change that measured it — `--claude-config-dir` sets `CLAUDE_CONFIG_DIR` on every Claude launch, the manifest records it, and `model-discovery` refuses to run without an empty one. First isolated captures promoted as `claude/2.1.241/{model-discovery,command-discovery}`. **The empty-home requirement is `model-discovery` only, and that is the correction:** `--bare` does not isolate (ambient bare discovery lists a fifth model, `claude-fable-5[1m]`, configured on the capturer's machine and published in the 2.1.233 corpus), while an empty home *also logs the CLI out* — for `command-discovery` that is 42 commands and `tokenSource: "none"` against 46 under a credentials-only home, a different observation under the same name. **Still open:** no run scenario has been captured through the flag, so `tools: 62` stands; and `2.1.228`/`2.1.229`/`2.1.233` are ambient and unrepairable, their CLI versions no longer installed |
| D104 | **Hermes' real permission posture is unconfirmed live, and the ACP path enforces no sandbox and selects no mode for any agent.** Source-read only (hermes-agent 0.15.2 installed `acp_adapter/permissions.py` and `edit_approval.py`, not a capture): a dangerous shell command is gated unconditionally — no mode, flag, or session setting skips it; a file edit's default is `session/set_mode`'s `"default"` → edit-approval policy `"ask"`, with two other modes (`accept_edits`→`workspace_session`, `dont_ask`→`session`) this crate never selects. The PR6 review found the reason that matters more generally: `crates/harness/src/acp/session.rs` reads `RunRequest.runtime_mode` nowhere except as the `SessionStarted` event field, and `.sandbox()` nowhere at all — no launch flag (contrast `claude::run_launch`'s `--permission-mode`, `codex/mod.rs`'s approval policy), no `session/new` param, no `session/set_mode` call. `RuntimeMode::ApprovalRequired`'s `ReadOnly` sandbox and every declared mode's promised behavior are therefore unenforced for the whole ACP path, Grok included, not just Hermes | PR6 review (`feature/acp-approvals`), 2026-08-29 — found because `ApprovalRequired` and `FullAccess` were byte-identical on the wire for Hermes, so declaring both was reverted to `[FullAccess]` alone | ☐ open, **unowned**. Two separable closes: (1) a real Hermes session to check the source read against a live `session/request_permission` — blocked today by a Python 3.14 `collections.MutableSet` removal the installed 0.15.2 adapter has not patched around — then wire `session/set_mode` at session-open time (PR7's owned surface) so Comet can actually select a posture before widening `HermesHarness::capabilities()` past `[FullAccess]`; (2) a sandbox mechanism for the ACP path generally, since `SandboxLevel` is carried on `RunRequest` today but read by nothing in `acp/session.rs` — Grok's declared `FullAccess` is honest only because it was proven never to ask, not because anything sandboxes it. **A third, independent gap the same review surfaced was split out to its own row, D120, by the 2026-08-29 whole-branch review — a false-transcript-record risk deserves its own visibility rather than living inside a row about sandbox/mode selection** |
| D105 | **Subagent visualization is not built for ACP.** Native Claude/Codex draw a subagent card (`RowKind::SubagentCard`, D53/D57); the ACP path (Grok, Hermes) has no equivalent. Upstream carries this as `a46b7010e6` (`acp/subagent.rs`, 735 lines plus disk-tailed transcripts) — separable by file and by commit, and deliberately not ported | ACP parity plan, deliberately out of scope (task-10 brief, PR10) | ☐ open, unowned. Grok ships visibly thinner than native Claude here, on purpose — porting `a46b7010e6` through `scripts/sync-upstream.py` is its own slice once ACP subagent events are worth showing |
| D106 | **`pi` is dropped from this plan entirely; `HarnessId::Pi` does not exist.** `crates/proto/src/agent.rs:9-20`'s `HarnessId` enum has `ClaudeCode`, `Codex`, `Cursor`, `Grok`, `Hermes`, `Mock` — no `Pi` variant, so it did not ride PR1's `PROTOCOL_VERSION` bump | ACP parity plan §11, deliberately out of scope (global constraints, "What is deliberately NOT in this plan") | ☐ open, unowned. Needs `pi-acp` plus the pi CLI plus managed-npm-install machinery this fork does not have. Its own slice if wanted |
| D107 | **Grok and Hermes have no install or update surface.** `crates/engine/src/registry.rs`'s Grok row (`:442-450`) and Hermes row (`:452-460`) both carry `install: None, update: None`, where Claude and Codex have real ones from slices 3.1-3.2 | ACP parity plan, PR1/PR8, unowned by any task | ☐ open, unowned. Neither agent's CLI has a Comet-driven installer or updater wired; a user must install `grok`/`hermes` themselves and Comet only probes what is already on PATH |
| D108 | **The shared ACP decode has no `Plan`, `Error` or `Notice` arm — by design, not by omission.** `agent_client_protocol` 0.9.0's `sessionUpdate` union has no error/notice-shaped member at all; Claude and Codex only decode those because their *native* (non-ACP) protocols carry dedicated push frames ACP does not. A `plan`-shaped update falls through to the generic Tier-3 Unknown-diagnostic catch-all in `acp/normalize.rs` rather than a typed checklist card — zero evidence it has ever arrived, across three real Grok captures plus a live probe | PR4 task report (`task-4-report.md`), 2026-08-28 | ☐ open, unowned, low priority absent evidence. Revisit only if a `plan`-shaped `sessionUpdate` is ever actually captured from any ACP agent; until then this is a correctly-absent decode, not a gap |
| D109 | **Grok has a real, unbuilt `Notice` source outside the shared `sessionUpdate` decode — vendor JSON-RPC methods, not a session update.** `_x.ai/announcements/update` and `_x.ai/settings/update` (captured 2026-08-28, `acp-raw-2026-08-28-run-steer`, every occurrence identical in shape) carry `{message, severity, title, cta}` on the `announcements[]` array — exactly `Notice`'s shape (`NoticeKind`'s `#[serde(other)]` Info arm covers the kind). **Severity needs a deliberate decision, not a default decode**: `NoticeSeverity`'s `#[serde(other)]` sits on `Warning`, not `Info` (`crates/proto/src/agent.rs:1245-1249`, doc comment: "unknown severities degrade LOUD... the opposite direction from `NoticeKind`") — so a naive decode of the observed `severity: "promo"` produces an amber warning chip on what is a marketing banner, not the `Info` reading a builder would want. Whoever builds this needs to map `"promo"` (and any other non-`"warning"` value Grok sends) to `Info` explicitly rather than relying on the default arm. Fires 2-3 times per captured run (`announcements/update`) plus once or twice (`settings/update`, which also carries a `tips: [...]` array) | PR4 task report (`task-4-report.md`), 2026-08-28 | ☐ open, unowned. Vendor-namespaced (`_x.ai/*`), so it belongs in `acp/grok.rs`, not the shared `normalize.rs`/`session.rs` — buildable independently of D108 whenever ACP `Notice` support is next picked up |
| D110 | **No supported-version floor exists for Grok or Hermes.** `docs/testing/supported-provider-versions.md` names a floor for Claude Code and codex-cli only; there is no corpus directory to point at for either — Grok has three sanitizable captures but none promoted (D102's remaining half; the dotted-key rejection that used to block it outright was fixed 2026-08-29), and Hermes has no capture recorded at all (only `initialize` was reachable live; see D104's own blocker) | ACP parity plan, PR10 closeout, 2026-08-29 | ◐ **Grok's half closed 2026-08-29**: `grok/1.0.5/` is promoted and the table names it. Hermes' half stays open and stays blocked behind D104 — no capture exists to point a floor at, and the doc now says that in place of leaving the row silently absent. Neither floor is enforced by anything (D69) |
| D112 | **A reasoning-only ACP turn emits a message-boundary event Codex would not.** `crates/harness/src/acp/session.rs`'s per-turn `streamed_this_turn` boundary fires on either `TextDelta` or `ReasoningDelta`; Codex only fires `AssistantMessageCompleted` on a completed `agentMessage` item, never on reasoning alone | PR4 task report (`task-4-report.md`), 2026-08-28 | ☐ open, unowned, low priority. ACP has no in-band "message segment finished" push distinct from turn end (`normalize::session_update`'s own doc comment), so the turn boundary is the most honest marker this build can emit — the divergence from Codex is a real behavioral difference, not obviously a bug |
| D120 | **Split from D104's third clause: Hermes' 60s permission auto-deny can leave Comet recording an action that never happened.** Hermes' `make_approval_callback` (`acp_adapter/permissions.py:112,150-155`) auto-denies an unanswered `session/request_permission` after a fixed 60s; the spawned task `acp::session::handle_permission_request` hands the request to has no bound of its own, and neither does Comet's approval card. Click Allow on that card at 61s and Comet writes `Allowed` into the transcript for a permission Hermes already denied — a **false record**, not a stale one, and this repository treats a false persisted transcript record as a first-class defect elsewhere (D57's own reasoning: "a stale `Running` reading beats a manufactured [`Cancelled`] one"). Dormant only because Hermes cannot run yet (D104's blocker 1: the installed 0.15.2 adapter hits a Python 3.14 `collections.MutableSet` removal) — the risk is that a future "Hermes works now" slice closes D104's other two clauses and ships this one unnoticed, buried inside a row already `☑`'d for a different reason | ACP whole-branch review, 2026-08-29, splitting the finding D104 (PR6 review, 2026-08-29) already made | ☐ open, unowned. Closing it needs either Comet to mirror Hermes' 60s budget so the approval card expires in step, or a real 60s+ Hermes turn to confirm the exact number and behavior first — same two options D104's own text named before the split, carried over verbatim rather than re-litigated. **Whoever closes D104's other two clauses must not close this one by accident**: it is a distinct mechanism (a timing race on the approval card, not a missing sandbox/mode selection) and needs its own fix |
| D127 | **`steer-grok`'s promoted capture is 560 KB, and 227 KB of that is one frame.** `crates/capture/tests/corpus/grok/1.0.5/steer-grok/events.jsonl` frame 429 is a `tool_call_update` whose `rawOutput.stdout` is a 20,416-element array — every element a distinct redacted placeholder, because the turn's prompt let Grok's `grep` run unbounded over the capturing machine. Six times the largest previously committed capture, and the bulk carries no information: the paths it evidences are already evidenced by the frame's own siblings | Grok promotion review, 2026-08-29 | ☐ open, unowned, low priority. Promoted anyway rather than deferred, because `steer-grok` is the only evidence of the tool-call surface, `rawInput`'s named children and the queued-steer shape, and hand-trimming staged output is forbidden (`provider-captures.md`). The fix is a re-record with a bounded prompt, which costs a live authorized turn; a re-recorded row is a new promotion, never an edit of this one |
| D128 | **Grok's permission surface has never been observed, and what it demonstrably emits instead is undecoded.** No promoted Grok capture contains a single `session/request_permission` — the method PR #131's approval bridge is built on — while `steer-grok` carries 17 `pending_interaction` (`kind: "permission"`) and 17 matching `interaction_resolved` session updates, both absent from the shared ACP decode. The capture does not settle which it is: those 17 were read-only tools Grok auto-allows, so it may still ask the client for a gated write | Grok promotion review, 2026-08-29, from the `sessionUpdate` vocabulary the new sheet made visible | ☐ open, unowned. Same family as D104's Hermes clause and the reason is the same: the approval path for both ACP agents is written from source and spec, never from a live permission prompt. Closing it needs one authorized turn that provokes a gated operation, not more reading |
| D131 | **A full-workspace run on a loaded developer machine fails in tests that pass in isolation, and the failing test is a different one each time.** Two instances on 2026-08-30, both during a four-agent parallel build: `comet-engine tests::concurrent_recovery_agrees_on_one_id` (6.8s under load) and `comet-capture record::tests::recorder_records_an_isolated_claude_config_dir_and_redacts_its_path` (0.08s quiet). Each passed **3/3 in isolation** immediately afterwards, on the same commit. Neither is in the diff of anything that was running | observed 2026-08-30 while gating the D59/D77/D114 batch | ☐ open, low priority, **unowned**. Filed so the next person meets a citation rather than re-deriving it, which is why D88 exists too — and per this file's "name the record or count the runs" rule, the counts above are the record. Same family as D88 (`instance_lock` on Linux CI) and D89 (a 25ms spawn budget, fixed by widening it): **the mechanism is a wall-clock budget meeting a saturated machine, not a race in the code under test.** What is missing is knowing whether these two have their own budgets to widen, the way D89 did, or are genuinely time-independent. **Do not conflate with D92**, which is a *hang* of the `comet-engine` lib test binary and is undiagnosed; these two fail and name themselves |
| D133 | **The ACP adapter gets Unix's process-tree cleanup for free and Windows' not at all.** D46 ([PR #197](https://github.com/matty/comet/pull/197)) reached the whole provider tree two ways: on Unix by spawning into a process group and signalling it, which every adapter inherits through the shared `shutdown_child`; on Windows by assigning the child to a Job Object at spawn, which only the Claude and Codex adapters do. `crates/harness/src/acp/session.rs` was left untouched, so a cancelled Grok or Hermes turn on Windows can still leave a provider-owned grandchild running | D46's own close, [PR #197](https://github.com/matty/comet/pull/197), 2026-08-30 | ☐ open, unowned. **Not a design question — the design is settled and shipped**, this is the same `ProcessTreeJob::attach` call at the ACP adapter's own spawn site plus the `TerminateJobObject` escalation, which is why it is a row rather than a slice. It was left out of #197 only because `acp/session.rs` belonged to a different change in flight at the time. Worth doing with the same falsification D46 used: a fixture that spawns a real grandchild and then hangs, so the leak is observable rather than assumed. Note Windows ships and CI is Ubuntu-only, so nothing in the PR gate can prove this one either way |

## Closed

**This table already mixes two conventions, and the five rows moved here 2026-08-29 follow
the minority one on purpose — said plainly rather than left for a reader to notice.** Most rows
without a standalone `D<nn>-*.md` page (D1, D2, D6, D60, D61, D79, D80, D85, D86) had their full
write-up moved into [`closed.md`](closed.md), leaving only a short pointer row here — the
majority pattern this file's "How to keep it" section describes. **D24 is the one existing
exception**: no page, no `closed.md` entry, full text stays inline. The five rows moved in this
pass — D73 and D75 keep linking their own existing standalone pages (matching D5/D11/D12/D101);
**D64 and D89 have no page and were left fully inline, following D24's precedent rather than the
majority `closed.md` one**, because moving their prose into `closed.md` was judged not to add
anything a reader here does not already get from the row itself. Every one of the five is
otherwise a pure relocation: content byte-identical to its prior Open-table row, only the table
section changed.

**A second pass on 2026-08-30 moved D52 and D53 here**, four days after slice 4.4 merged them
([PR #115](https://github.com/matty/comet/pull/115)); both had sat in the Open table carrying a
"closes on merge" state. D53 arrived malformed, and D89 already here was malformed the same way:
**a five-cell row in a four-column table**, where an updated state had been appended as a new cell
instead of replacing the old one. GitHub renders only the first four cells, so in both cases the
update was invisible on the rendered page and the superseded state was what a reader saw — D53's
cost a session a wrong recommendation to re-plan a slice that had already shipped.
`crates/engine/tests/debt_index_shape.rs` now fails on any such row.

| # | Item | Origin | State |
| --- | --- | --- | --- |
| D64 | ~~`comet-provider-sanitize` does not redact model-authored prose inside tool-call inputs or results.~~ It named the blocklist-era gap where a `TaskCreate` input's `subject` and `description`, and a `TaskUpdate`'s `activeForm`, passed through **verbatim** because the redactor had no rule for them, and generalized it to any tool-input field like them. **The general mechanism is closed**: the allowlist-sanitizer stage inverted the default, so a field nothing on `claude.txt`/`codex.txt` names is now redacted rather than surviving by omission — re-sanitized, the same `checklist` capture now shows `<V210>`/`<V209>`/`<V240>` at those three fields, not the literals, and `allowlist_property.rs`'s total property proves this holds over every committed frame, not just this one | found reviewing the staged `checklist` captures, 4.3; general mechanism closed by the allowlist-sanitizer stage, 2026-08-15 | ☑ merged. The blanket "no rule means it survives" defect closed 2026-08-15; its residual — [D73](D73-tool-argument-union-paths.md)'s seven tool-argument-union paths — closed in the stage-6 promotion (`test/stage-6-promotion`), so nothing is left open under this row |
| [D73](D73-tool-argument-union-paths.md) | **Seven allowlisted paths are a tool-argument union, not a field each.** `.message.content[].input.status`/`.taskId`/`.type` and `.tool_use_result.matches[]`/`.status`/`.type`/`.updatedFields[]` hold whichever fields the *currently invoked* tool defines — already a union of five tools in the committed corpus (`TaskCreate`/`TaskUpdate`/`Read`/`ToolSearch`/`SendMessage`), all reviewed before the paths were approved | Task 2 review, 2026-08-15 | ☑ merged, stage-6 promotion (`test/stage-6-promotion`). The seven lines are dropped from `claude.txt`; every position now redacts by default like any other unlisted path. **Read the page's "Closed" note**: the two `corpus_frames.rs` tests this broke needed a real rewrite, not the no-op an earlier comment claimed |
| [D75](D75-read-back-files-outside-the-allowlist.md) | **`tests/corpus/` holds two redaction standards, and only one is enforced.** `claude/2.1.229/subagent/read-back-doc-snapshot.json` and `read-back-run-journal.jsonl` were hand-sanitized by an earlier slice (4.2 task 9) under the pre-allowlist vocabulary and were never in `sanitize_dir`'s remit — `allowlist_property.rs`'s own doc comment says so. They still publish full assistant/reasoning prose, a ~35-entry tool roster, and `agentType: "general-purpose"`, a member of the exact subagent-roster shape this stage stripped from `events.jsonl` everywhere else | Task 6 sweep, allowlist-sanitizer stage, 2026-08-15 | ☑ merged, corpus-hygiene cleanup (`test/corpus-hygiene`). Option 2 chosen: both files deleted rather than sanitized under their own allowlist — nothing in `crates/` loaded them as test data. **Read the page's "Closed" note** for what they held and why deletion was the closure picked over building a second enforced sanitizer |
| D89 | **`codex_startup_timeout_never_applies_to_an_active_turn` budgets 25ms of wall clock for a real process spawn, so it fails whenever the machine is busy.** `crates/harness/tests/codex.rs:202` sets `with_startup_timeout(Duration::from_millis(25))` and then expects `AgentEvent::SessionStarted` from a genuinely spawned fixture executable. Windows process creation alone is commonly 10–30ms unloaded, so the budget has almost no headroom: under CPU saturation the startup timeout fires before the handshake lands and the run fails before the assertion it exists for is ever reached. The timeout value is the *subject* of the test — it must stay small enough that an active turn outliving it proves something — so this is a real tension, not a typo | three independent observations on 2026-08-16, during parallel worktree builds | ☑ **fixed the same day, and the "16/16 idle" figure below was wrong.** Measured with 12 isolated runs per branch on a quiet machine: `origin/main` **10 passed / 2 failed**, a feature branch **5 / 7**. The earlier 16/16 was luck, and the between-branch spread is small-sample noise, not a regression — a wrong baseline nearly got a real-looking failure written off, and then nearly got a flake reported as a regression. The fix takes the resolution this row already named: raise the startup budget to 500ms and lengthen the active-turn observation window to 2000ms, holding the 4x ratio. **The property is unchanged** — what proves it is the window exceeding the startup timeout, never the timeout being small. 12/12 then 10/10 green after. Verified the knob is still live rather than inert: a 1ms budget still fails at the `SessionStarted` assertion. **Not falsified end to end** — the fixture holds its turn open until interrupted, so widening the window produces no `Done` to catch, and proving the deadline cannot leak into an active turn needs production sabotage nobody has done — **the evidence this corrected, kept verbatim because the correction is to it:** **Evidence, per this file's "name the record or count the runs" rule:** no record existed anywhere in `docs/` or `.agents/`. Two agents hit it independently in full-workspace runs while four cargo builds saturated this machine; one confirmed via `git stash` that it also fails on an unmodified tree (1/3 isolated runs at `wmic cpu get loadpercentage` = 100), so it is not caused by any branch in flight. On an idle machine it passed **16/16** across two branches. So the trigger is load, and the mechanism is the 25ms budget, not a race in the code under test. **It was dismissed once as "just load" before the third observation arrived** — which is why it is recorded now rather than re-derived by whoever meets it next, probably on a shared CI runner |
| [D101](D101-rpc-tests-watcher-delete-race.md) | **`comet-engine`'s `rpc::tests` module hangs intermittently — a different test each time, and it can wedge the whole gate.** Measured on `main` at `1f0b20c4`, Windows: `cargo test -p comet-engine --lib rpc::tests` hung **2 runs in 6**, on `stale_finalizer_cannot_retire_a_new_generation_session_or_journal` and `rejected_resume_retry_cannot_register_against_an_idle_successor_generation`; a separate session saw `deleting_a_running_chat_refuses_a_late_tool_diff` and `restarted_same_id_reconciliation_retires_the_old_journal_before_dispatch`. A passing run takes **2.4s**; a hanging one never returns. **The victim varies, which is the signal** — these are not four broken tests but one shared race, and every one of them drives the session/journal/generation dispatch machinery. Any single test run alone passes 6/6 in 0.1s, so it needs the module's parallelism | found when `cargo test --workspace` hung twice during the Grok slice, 2026-08-28; bisected to `main` and therefore NOT that branch, whose changes are confined to `crates/harness` | ☑ closed, fixed in PR #128 (`fix/d101-engine-hang`). A Windows-only race between `tempfile::TempDir::drop()`'s recursive delete and a `notify` filesystem watcher `SpacesSync` (`crates/engine/src/spaces.rs`) arms on a space's folder — three independent call sites fed it a real, existing directory (the test's own tempdir), found one at a time across two review rounds, the second of which corrected a wrong "second bug" claim from the first and caught a third route live with a dedicated stress-and-WinDbg campaign. All three now route through one helper that points at a path nothing creates, so `notify::Watcher::watch` fails before any OS handle opens — structural, not probabilistic — guarded by a `debug_assert!`. Full mechanism, the corrected history, the run counts, and one hang that stress testing could not explain under any found mechanism: `D101-rpc-tests-watcher-delete-race.md`. Step 5 ruling: a test artifact, never a production RPC-dispatch deadlock. `SpacesSync`'s missing synchronous stop API is a real, separate gap — see D103 (Open table above; this row moved to Closed on 2026-08-29, so "below" no longer holds).|
| D116 | **`titles::generate` renamed the worktree branch before confirming the title write landed; its sibling deliberately did not.** `generate` called `rename_worktree_branch_for_title` and only then wrote the title via `rename_chat_auto` — if the write failed or lost the first-writer-wins race (the function's own re-read of the chat exists for exactly that race), the branch had already been renamed for a title that never landed. Its sibling `apply_agent_title` did the opposite on purpose: it writes first, reads `title_landed` back, and renames the branch only if the write actually landed | PR10 closeout review, 2026-08-29 | ☑ merged [PR #143](https://github.com/matty/comet/pull/143). `generate` reordered to write-then-rename, matching its sibling; on a failed or raced write the branch now keeps its old name. Kept here rather than deleted because `crates/engine/src/titles.rs` cites **D116** by number in the two doc comments that carry the reasoning, as does the regression test `generate_never_renames_the_branch_for_a_title_that_lost_the_write_race` (`crates/engine/tests/m5c_accounts_uploads_titles.rs`) |
| [D1](closed.md) | Empty `ReasoningDelta` while parked flips the session to Working and permanently disarms the 30-min idle reaper | 0b.1 whole-branch review | ☑ merged [PR #27](https://github.com/matty/comet/pull/27) |
| [D2](closed.md) | "Write the `None` case yourself" — optional wire fields need their absent case written by hand; a plan's fixtures will not generate it | 0b.1 Greptile P1 | ☑ merged [PR #28](https://github.com/matty/comet/pull/28) → `.agents/rules/optional-wire-fields.md` |
| [D5](D05-hung-tool-call.md) | A Codex tool call can hang **forever** with no timeout and no recovery — observed live, 12+ minutes on one exec call, session pinned Working | 0b.2 real-CLI check | ☑ merged [PR #36](https://github.com/matty/comet/pull/36) (`5a8a16e`). **Read the page's "what closed means" note before treating it as gone** |
| [D6](closed.md) | Claude's unclaimed `control_request` subtypes are counted but never answered; `sdk.d.ts` says hosts must reply `{behavior:"cancelled"}` to unrecognized dialog kinds | 0b.2 sink 3 | ☑ closed by 1.6 ([PR #37](https://github.com/matty/comet/pull/37), `61fec51`). Written **blind** — nine capture runs produced zero non-`can_use_tool` control requests. The diagnostic was kept: answering a request does not make it understood |
| [D11](D11-remembered-request-mode.md) | A run's remembered `last_request` carries the **previous** turn's runtime mode; three paths preferred it over the chat row, so a mid-chat mode change would not reach a steered or resumed run | 1.1 whole-branch review | ☑ closed by 1.8 ([PR #39](https://github.com/matty/comet/pull/39)). `DocHost::apply_chat_row_runtime_mode` overlays the row's current mode on all three paths and re-derives the sandbox from it. **The ruling the page records: a mode change applies to the next dispatch, never to a run in flight** — the provider process was spawned with its permission mode |
| [D12](D12-permission-axis-version.md) | The permission axis crosses RPC with no version signal: two same-`PROTOCOL_VERSION` builds can pair, and the older one silently runs a request under the default mode | 1.2 Greptile review, premise verified | ☑ closed by 1.8 ([PR #39](https://github.com/matty/comet/pull/39)). `PROTOCOL_VERSION` is `4`, so a mixed pair is refused outright rather than running a session under a mode nobody chose. Note this is **not** the usual decode-driven bump: the field was always additive, but its absence is indistinguishable from a deliberate value |
| D24 | **The deny note never reaches Codex, and the field said it would.** Comet persists it, but Codex's decision literals have no message field, so the wire carries `"decline"` and nothing else | 1.7 live check, scenario 3 | ☑ closed by 1.8 ([PR #39](https://github.com/matty/comet/pull/39)). `HarnessCapabilities::carries_deny_note` (conservative default `false`) drives the field's label, so a provider that cannot deliver the note no longer has a field promising it. **The note is still undeliverable on Codex — nothing invented a channel.** D14's residual is the mirror image and stays open |
| [D60](closed.md) | **`comet-provider-capture --help` can advertise a scenario the binary refuses.** The scenario name lived in three unsynchronized places — the help text, `supported_pair()` and the dispatch match | found adding the checklist scenarios, 4.3 | ☑ merged, neutral-recorder stage ([PR #67](https://github.com/matty/comet/pull/67)). `crates/capture/src/record/scenarios.rs`'s `SCENARIOS` table is now the one place a scenario's name, provider, purpose and requirements are declared |
| [D61](closed.md) | **A capture's evidence guard encoded the prompt's content, from a different file.** `recording.rs`'s Claude run loop required two distinct confirmed creates plus one confirmed update for `checklist`, and one update to a task it had never created for `checklist-resume` — thresholds correct only because `capture/checklist.rs`'s prompt happened to ask for exactly that much. (This row said "4 and 2" until the guard was deleted: those were the *prompt's* tool-call counts, not the predicate's — see `closed.md`.) | found adding the checklist scenarios, 4.3 | ☑ merged, neutral-recorder stage ([PR #67](https://github.com/matty/comet/pull/67)). The guard is deleted, not fixed: design §3.2 removed every frame check that aborts, and this was exactly that class |
| [D79](closed.md) | **Codex fence selection was derived from `runtime_mode`, not declared by the scenario row.** `record.rs`'s `codex_fence` picked the trusted-PowerShell/cwd-identity branch on `spec.runtime_mode == Some(RuntimeMode::ApprovalRequired)` rather than reading a field the row states outright | final fix wave, neutral-recorder stage, 2026-08-15 | ☑ closed, `scenario-request-builders` Task 4 ([PR #77](https://github.com/matty/comet/pull/77)). `ScenarioSpec` now carries `fence` directly; `no_fence` is the eighteen-row default and the two Codex approval rows name `codex_fence` explicitly. **Residual, found by this task's own falsification, closed by the same branch's fix pass**: a loop in `record.rs` (merged with the run-builder purity/wiring loop by a same-day review-fixes pass into `every_row_s_builder_and_fence_match_its_declared_wiring`) checks every row's `fence` against an exhaustive expected-kind table, fingerprinted by behavior rather than function-pointer identity. See `closed.md` |
| [D80](closed.md) | **`claude model-discovery` and `model-discovery-neutral-cwd` are byte-identical scenario rows.** Both set the same `runtime_mode` (`None`), the same `Requirements::discovery()`, the same `launch` (`claude::model_discovery_launch`) and the same `body` (`claude::model_discovery`) — the only difference anywhere in the table is the `name` string and the `purpose` prose. They are one scenario recorded twice under two names, not two independent observations of Claude's behavior | final fix wave, neutral-recorder stage, 2026-08-15 | ☑ closed by live re-capture, 2026-08-16 (`test/stage-6-promotion`). Claude 2.1.233 and Codex 0.147.0 re-captures showed all three model-discovery rows per provider differing only by a redacted `pid` (Claude) or byte-identical (Codex) — `--bare` discovery is cwd-independent for both, and D32's cited counter-evidence belongs to `command-discovery`, a different scenario with a different launch. `model-discovery-neutral-cwd` and `model-discovery-project-cwd` are deleted for both providers, their corpus directories removed, and the capability sheets regenerated. `model-discovery-logged-out` is untouched — it varies by auth state, not cwd. See `closed.md` |
| [D85](closed.md) | **The sheet named a field but not which scenario produced it.** `claude/2.1.228` and `claude/2.1.229` share zero scenario names, so their `git diff --no-index` — the whole version-change mechanism — showed ~200 insertions and ~200 deletions describing different scenario coverage, not different CLI behaviour, and roughly a third of each sheet was caveat prose apologizing for exactly that gap. `FieldObservation::first_seen` computed a scenario for each field and was read nowhere outside `surface_map.rs`'s own pinning tests | Task 5, capability-sheets stage, 2026-08-16 | ☑ merged, sheet-scenario-attribution stage (`test/sheet-scenario-attribution`). `FieldObservation` grew a full `scenarios: BTreeSet<String>`, rendered as a `G<n>` scenario-group index in the Fields section — the header's and Fields section's apology prose is cut accordingly, and the Vocabulary section (no per-value attribution) keeps its own |
| [D86](closed.md) | **The sheet could not show tool-roster growth.** The `system`/`init` frame's `tools` array is redacted field-by-field, but its length survives — 29 in every `2.1.228` scenario, 35 in `2.1.229`'s `subagent`, 59 in `checklist`/`checklist-resume` (the two MCP-connected scenarios) — and the sheet rendered none of it, so a real roster change was invisible to the version-change report | whole-branch review, capability-sheets stage, 2026-08-16 | ☑ closed, sheet-tool-count slice. `SheetScenario` carries a `tool_count: Option<usize>` rendered as one `tools: N` / `tools: (not observed)` line per scenario, sourced from the archive's own `system`/`init` frame length, never from a tool name inside it. `git diff docs/providers/claude-2.1.228.md docs/providers/claude-2.1.229.md` now shows the 29 → 35/59 change directly |
| D113 | **`session_update_once`'s dedup `HashSet` grows unbounded across a session's lifetime.** `crates/harness/src/acp/normalize.rs`'s `seen: &mut HashSet<String>` (caller-owned, session-scoped) had no cap, unlike `ToolTracker`'s `MAX_TRACKED_CALLS` | PR4 task report (`task-4-report.md`), 2026-08-28 | ☑ merged [PR #141](https://github.com/matty/comet/pull/141). Capped at `MAX_TRACKED_UPDATE_KINDS = 64`. **Kept here rather than deleted because the code cites the number**: the const's own doc comment is the record of why `ToolTracker`'s strategy could not be copied — refusing to insert while still reporting would make every kind past the cap look newly seen on every frame, which is the warn storm the dedup exists to stop. The cost is stated there too: past the cap the kinds that arrive are lost to everything but `trace!`. What is NOT given up is knowing that happened — the first kind past the cap emits one `sessionUpdate/reporting-capped` diagnostic per session, so a reader can tell "this agent sent nothing unfamiliar" from "Comet stopped looking". A latch entry in the same set makes it once-only, and it holds a space so no agent-chosen kind can mint it |
| D121 | **The notification-settle arm's post-harvest path never re-drained `incoming`.** `crates/harness/src/acp/session.rs`'s harvest awaits the in-flight `session/prompt` reply under `POST_NOTIFICATION_REPLY_BOUND` (250ms) before returning `TurnEnd::Settled`; anything the reader task pushed into `incoming` during that wait was left undrained, because the two existing per-arm drains both run BEFORE the harvest, not after it | ACP whole-branch re-review, 2026-08-29 | ☑ merged [PR #141](https://github.com/matty/comet/pull/141). A third `drain_buffered` call after the harvest, before the usage event and the `reason` read. **Kept here because the test that pins it is timing-shaped and its fixture says so**: `fake_acp.rs`'s `complete-race-drain` sleeps 50ms between the notification and the raced-in chunk, and at the 2ms it was first written the test passed against the broken code 3 runs in 7 — the client's own pre-harvest drain scooped the chunk whenever it was descheduled. Do not shrink that gap |
| D123 | **`MAP_PATHS` was all-or-nothing per path, so one declaration disabled the capability-sheet golden test for two fields production actually decodes.** Declaring `.params.update.rawInput` a map is what stops every tool's own argument names publishing as reviewed field names, but `Visit::walk` then folded every child to `.{}` — including `pattern` and `path`, which `acp/normalize.rs`'s `typed_call` reads on a `search`-kind frame. Closed by giving each `MapPath` an opt-in `named_children` list: a named key is walked as an ordinary field and keeps its spelling through sanitizing, every unlisted sibling still folds. **The open row's parenthetical "`pattern` and `path` are already reviewed allowlist decisions" was wrong and is corrected here** — neither is on `allowlist/acp.txt`, and naming a child deliberately does not put it there: the key becomes a field name, the value stays default-deny. **Latent on merge**: no promoted scenario exercises `.params.update.rawInput` at all, so every sheet regenerated byte-identical and the declaration only takes effect the day such a scenario is promoted — until then `normalize.rs`'s own `pattern`/`path` decode test is still the only cover, and that test's doc comment says so | the code's own comment at `surface.rs` stated it; raised again by the `/simplify` altitude review, 2026-08-29 (finding 6), deferred by [PR #138](https://github.com/matty/comet/pull/138) | ☑ merged [PR #142](https://github.com/matty/comet/pull/142) |
| [D87](D87-capture-stays-coupled-to-harness.md) | **`capture/` compiled into production builds; the simplification design's stage 7 would extract a `comet-capture` crate that `comet-harness` only dev-depends on, so production *physically cannot* reach capture machinery** | stage-7 premise check, 2026-08-16; reopened against `codex-acp`/`claude-agent-acp`, PR10 (`docs/acp-closeout`), 2026-08-29 | ☑ merged, stage-7 crate split ([PR #145](https://github.com/matty/comet/pull/145)). `crates/harness/src/capture/` moved to its own workspace member, `crates/capture` (package `comet-capture`); `comet-harness` depends on it only as a dev-dependency, and `crates/harness/src/lib.rs` no longer declares `pub mod capture;`. `cargo tree -p comet -e normal` lists `comet-harness` but never `comet-capture` — production physically cannot reach it. Full mechanism, the visibility promotions this needed, and the `[[bin]]`-targets-never-get-dev-dependencies gotcha it ran into: see the page's "Stage 7 executed" section |
| [D102](D102-grok-capture-command-push-and-dotted-keys.md) | **Grok's command push ships with three fields unevidenced, and no Grok capture is promoted yet.** Two blockers: the push mixes Grok's own built-ins with every personal skill on the capturing machine (unfixable by any documented isolation lever), and `comet-provider-sanitize` refused every Grok capture over its `_meta["x.ai/..."]` vendor keys | ACP capture-promotion slice, 2026-08-28 | ☑ merged. Blocker 2 fixed by escaping (`surface::escape_path_segment`); Blocker 1 stands as a **ruling** — the command push is promoted as placeholders on purpose, and the page says why, so nobody "fixes" it by allowlisting three decoded fields. All three captures are in `crates/capture/tests/corpus/grok/1.0.5/` with `docs/providers/grok-1.0.5.md` beside them; the page also records the one allowlist line the review added and the two mechanism bugs the first escaped path exposed. Residual, low priority: a capture on a clean machine is still the only thing that would separate Grok's own command surface from an operator's tool inventory |
| D124 | **`prompt_completions` is the uncapped sibling D113 just closed.** `crates/harness/src/acp/session.rs:844`'s `let mut prompt_completions: HashSet<String> = HashSet::new()` is session-scoped and caller-owned, inserted at `:1218` from `result["_meta"]["promptId"]`, with no cap — the same shape as `session_update_once`'s `seen`, which now stops at `MAX_TRACKED_UPDATE_KINDS + 1` and reports once when it does. **Milder, and the difference is the axis**: `seen` grew per *frame* off a provider-controlled discriminator, so 64 junk frames could blind it; this grows one entry per *turn* off a prompt id, so a session would need thousands of turns to matter. Recorded because closing one and walking past its sibling is how the next reader concludes the pattern was considered and rejected | PR #141 review, 2026-08-29, raised and deliberately left out of scope to keep that branch reviewable | ☑ merged. `PromptCompletions` (`acp/session.rs`) evicts its OLDEST id at `MAX_TRACKED_PROMPT_COMPLETIONS = 64` — neither sibling's strategy would do, and the const's doc comment is the record of why: `ToolTracker` refuses to insert and D113 treats everything past the cap as already-suppressed, both of which KEEP the oldest entries, while the id this guard actually needs is the one just inserted (a duplicate completion rides one push behind the reply that beat it). No escalation event, unlike D113: nothing observable is given up, so a `trace!` is the honest weight |
| D125 | **Nine files cite `DEBT.md`, a file that does not exist.** `crates/engine/src/{approvals,doc_host,sessions}.rs`, `crates/engine/tests/e2e.rs`, `crates/harness/src/acp/normalize.rs`, `crates/harness/src/codex/{approval,mod}.rs`, `crates/proto/src/agent.rs` and `crates/ui/src/composer.rs` carry comments of the form ``(`DEBT.md` D11)``. The debt record moved to `docs/debt/README.md`; the citations were never repointed, so a reader chasing one finds nothing at the named path. The D-numbers themselves still resolve — this is a stale *path*, not a dangling reference | found during the 2026-08-29 debt batch, after three PRs in it independently hit the inverse problem (rows deleted while code still cited them by number) | ☑ merged. All 16 citations across those nine files repointed to `docs/debt/README.md` uniformly — not to each row's own page, so there is one rule and no per-site judgment; the README row links the page where one exists. **A guard came with it**: `crates/engine/tests/debt_citations.rs` fails on a cited `docs/debt/*.md` path that does not exist AND on a cited `D<nn>` that is in neither `README.md` nor `closed.md`, which is the inverse problem (a row deleted while code still cites it) the same batch hit three times. 36 distinct numbers and 4 paths are cited across the sources today; all resolve |
| D99 | **The mock harness declares `supports_steering: true` and never drains `controls.steering`.** `crates/harness/src/mock.rs:91`. A steer sent to it is silently dropped and the run just ends; only the Claude and Codex adapters emit `AgentEvent::Steered` | 4.4, found while trying to reach D57's state from the mock, 2026-08-26 | ☑ merged. The run now drains `controls.steering` on the same wait that paces the script, emits `Steered` with rotated ids of its own (the mock has no provider ids to rotate) and a line of prose so the post-steer segment is not empty. **D57's `last seen running` subagent state is reachable from the rig as a result** — `COMET_MOCK_SUBAGENT=1`, a `COMET_MOCK_DELAY_MS` high enough to type into, then steer — so `mock-states.md` moved it out of "States no knob reaches" and into the recipe list. Unpaced, the script still outruns a human and the agent settles `Cancelled`: the script finishing first, not the drain failing |
| D95 | **`valid_or_default` accepts any unknown key name, so a persisted combo can bind nothing and say nothing.** `crates/ui/src/shell.rs`'s guard passes a combo through `Keystroke::parse`, which (read at the pinned gpui rev `ac135eb`, `crates/gpui/src/platform/keystroke.rs`) rejects only an unknown *modifier*: `ctrl-nosuchkey`, `zzz`, `ctrl-` and `""` all parse, the last two with an empty `key`. A hand-edited `ui-settings.json` therefore binds a shortcut that can never fire, with no warning and no fallback to the default the guard's own name promises. #105 widened the exposure by nine persisted `jumpSession` slots | named as "worth a debt row" in [PR #90](https://github.com/matty/comet/pull/90) and never filed; re-verified against gpui in the 2026-08-25 upstream-batch review | ☑ merged. `is_emittable_key` (`shell.rs`) checks the parsed key against the named-key vocabulary the platform layers actually emit — read off the pinned fork rev `ac135eb` (`gpui_{windows,macos,linux}` events sources), not guessed, because gpui's own `is_printable_key` is private and inverted. A single character passes whatever it is (`[`, `/`, a dead `-` are real on some layout); what is rejected is the shape a typo takes — a multi-character name nothing emits, and the EMPTY key `ctrl-` and `""` both parse to. Three tests: the fallback direction, the keep direction (so the guard cannot become reject-everything), and every shipped default passing its own guard |
| D84 | **The headed app ignores `COMET_HARNESS` when choosing a new chat's provider.** Its embedded engine resolves the variable, but `apps/comet/src/main.rs` hardcodes `UiConfig.default_harness` to Claude Code. A mock-render run can therefore pair model `mock-1` with Claude and show “The selected model isn't available” until the chat has an explicit Mock config and the app restarts | tree-sitter sync rendered QA, 2026-08-16 | ☑ merged. `EnvDefaults::from_env()` is the single read; `into_ui()` and `into_engine()` are the only two ways a config gets built, so the headed arm can no longer hardcode anything. The two configurations still differ in what they CARRY (`unattended_timeout` is the engine's alone) — what they no longer differ in is what they READ. Covered at the call site as the row asked, and by the property rather than a value: both configurations carry the same harness, for every `HarnessId` variant, so a new one cannot be added to `harness_from_env` and missed on one side. The other three shared fields (`data_dir`, `ipc_port`, `releases_url`) get the same test — a UI pointed at a different data directory than the engine it embeds is the same class of bug |
| D119 | **A failed `config_requests` call in `AcpSession::open` is a new raw-provider-text site — the same RULE 1 violation D4 already tracks, reached through a different mechanism.** `crates/harness/src/acp/session.rs:289-296`'s loop feeds `client.request(method, params)`'s error straight through `with_timeout` (`:680-692`) to its caller unchanged; `RpcClient::request` (`crates/harness/src/jsonrpc.rs:93-97`) builds that error as `HarnessError::Protocol(format!("{method}: {failure.message}"))` — the provider's own JSON-RPC message, verbatim, with no summary/hint split. `drive_run`'s error sink (`crates/engine/src/sessions.rs`) renders it to the user close to as-is. D4's own thirteen sites are `format!("{err}")` on a client-side `RpcError` in `crates/ui`; this is `HarnessError::Protocol` on the harness/engine side — a different type, the same "no raw error on screen" rule broken. Reachable whenever the picker's curated or cached model id disagrees with the account's live list — Grok answers `session/set_model: Invalid params: unknown model id` (`grok.rs:33,391`'s own doc names the exact wire text) | ACP whole-branch review, 2026-08-29 | ☑ merged. `setting_refused` (`acp/session.rs`) answers the summary/hint split, and the raw message is warn-logged at the refusal rather than dropped. **Keyed on Comet's own method string, never on the agent's message** — the message is the thing that must not reach a surface, so it is not read to decide the copy either. `OpenFailureMapper` was indeed the wrong tool and is untouched: its job is recognizing "needs sign-in" before a session exists; this is a session that opened and then refused a setting. The new `HarnessError::SettingRefused` is a second variant rather than a reuse of `NeedsSetup` because the user's next action differs (pick another model vs. go configure the CLI). Driven end to end by a `refuse-set-model` fixture trigger answering Grok's real wire text, and the test asserts that text does NOT appear in what the user sees |
| D100 | **The probe's `hint` is the one user-facing surface that still carries a raw error.** `probe_cli_version` (`crates/harness/src/lib.rs`) interpolates the `io::Error` verbatim in its "could not be started" arm — a real hint reads `... could not be started: The system cannot find the file specified. (os error 2)` — and dumps the CLI's own stderr in the `--version`-failed arm. `.agents/rules/user-facing-errors.md` rule 1 names both by name: "no OS error codes, no CLI stderr dumped verbatim". It reaches the models pane for every harness, not just the new one | found writing the Grok probe test, 2026-08-28; `crates/harness/tests/grok.rs` records the gap at its assertion | ☑ merged, and the decision went the way the row sketched. **The `io::Error` Display is gone**: `start_failure_hint` reads `io::ErrorKind` instead, so the hint names the path and an action (`NotFound` → check the path or clear the override; `PermissionDenied` → not runnable by this account) with no OS error code, and the full error stays in `tracing`. **The stderr passthrough is KEPT by ruling and narrowed**: `readable_detail` gives one line, trimmed, control characters replaced, capped at 160 with an ellipsis — not a dump, and a generic sentence would have lost the one actionable thing a half-finished install says. `a_failing_cli_reports_its_own_error` still asserts `boom` reaches the hint, deliberately, unchanged. `crates/harness/tests/grok.rs` recorded this gap at its own assertion and now leak-checks the hint alongside the summary |
| D126 | **`concurrent_recovery_agrees_on_one_id` can exhaust its retry budget on a saturated machine.** `crates/engine/src/lib.rs:732` spawns 8 threads into `load_or_create_device_id`, which retries `DEVICE_ID_ATTEMPTS` (24) times with a `DEVICE_ID_RETRY_PAUSE` of 3ms — about 72ms of budget. Exhausting it returns `EngineError::Other("could not establish a device identity…")`, which the test `.unwrap()`s into a panic. The budget is sized for exactly this contention (the const's own doc: Windows `rename`/`remove_file` fail with `ERROR_ACCESS_DENIED` while any other handle is open, and "8 concurrent openers reproduced it"), but sized on an unloaded machine — the same mechanism as **D89**, where a budget that held when idle failed under parallel builds | observed once in a full-workspace run while four other cargo builds saturated this machine, 2026-08-29 | ☑ merged. The count is gone: `DEVICE_ID_RECOVERY_BUDGET` is 5 seconds of WALL CLOCK with an exponential backoff (3ms doubling to a 50ms cap), so a loaded machine gets proportionally more attempts where a fixed count gave it fewer useful ones. Roughly 100 attempts against the old 24, and a test asserts exactly that comparison — the replacement is checked against the count it replaced rather than a number picked fresh. Backing off also breaks the lock-step convoy a flat 3ms pause made of every repairer. Five seconds on a startup path is bounded and only spent while repairing a corrupt identity under contention, in place of refusing to start; the error now names the attempts and elapsed time, so the next occurrence arrives diagnosed rather than as a bare sentence. **Attribution was never confirmed** — the original failing run's error text was not captured — so this removes the likeliest cause rather than a proven one, and D89 is the same shape still open |
| D96 | **Recovering a zero-byte `device-id` silently re-homes the machine, and every space created under the old id goes "offline" with no explanation.** #96's recovery is the right call — the old id is gone, and refusing to start was worse — but spaces are keyed by `deviceId`, so after recovery the sidebar renders each one `@ <host> · offline` and its chats cannot dispatch, because their host device no longer exists. Nothing tells the user why or what to do. Distinct from **D94**, which is the concurrent-repair race, not this consequence | reproduced live on Windows in the 2026-08-25 upstream-batch review: truncate `device-id` to 0 bytes, boot, watch the seeded spaces turn offline | ☑ merged, the SECOND way, and the first is closed off on evidence rather than preference. **Re-homing is not available**: `Space::device_id` is documented immutable, `checkout_id` is `sha256(deviceId ‖ git_dir)` so restamping invalidates every diff-grouping key, and the local store holds PAIRED PEERS' spaces too (`spaces.rs`'s own `device_id` filter is why) — a blind restamp would claim another machine's. So Comet says it instead: recovery writes a `device-id.rebuilt` marker, `LocalDevice` reports `identityRebuiltAt` (additive, no `PROTOCOL_VERSION` bump — an older peer that drops the key shows no notice, which is what every build before this did), and the sidebar carries an amber strip whose action leads: "Add older spaces again to use them". **It cannot name the affected spaces** — the old id died with the zero-byte file — so "spaces created before that" is the most precise true statement available. Dismissal is keyed on the stamp and persisted in `ui-settings.json`, unlike the update strip's per-boot one, because a rebuilt identity is permanent and a per-boot dismissal would nag for the life of the install. **Robustness is deliberately deferred** (user ruling, 2026-08-30): telling the user first, re-homing or per-space attribution later if it is ever wanted |
| D129 | **`deleted_checkout_is_evicted_after_grace` has 100ms of margin, and that is the third test in this family.** `crates/engine/tests/diff_sync_churn.rs` starts the sync with a 300ms orphan grace, calls `repair_now()` to mark the checkout orphaned, sleeps 400ms, then calls `repair_now()` again expecting removal. The margin is the 100ms between them: if the first repair's orphan stamp lands late under load, the second sees less than the grace elapsed and the entry survives. **Sibling of D89 and D126** — a timing budget sized on an idle machine — which is worth stating as a pattern rather than a third isolated row: this repository's timing-sensitive tests are written against the machine that wrote them | observed once during the D96 gate, 2026-08-30, with clippy and a build running alongside | ☑ merged, the way the row named: `wait_for_eviction` polls passes until the entry is gone under a 30s deadline that only ever bounds a failure, so there is no margin left to size wrong. **The negative half is kept explicitly** — one pass before the grace elapses must NOT remove the entry — because a poll alone would pass against a grace of zero, which is exactly what the probe showed. **A second site had the same 400ms-past-a-300ms-grace shape** and is fixed with it: `chat_flap_keeps_entry_and_sustained_absence_removes_it`, which this row named only one of. The file's two `sleep(3s)` calls stay: they prove a NON-event ("nothing was re-published"), where there is no condition to wait for. The standing rule is now in `.agents/workflows/verify.md` under "Writing a test that waits", with D89 and D126 named beside it — an idle developer machine is the least representative one this code runs on |
| D15 | An approval drained at a steer or by a session-grant sweep can be outrun by its own `ApprovalRequested`: the event still passes the authority guard (its id is in `minted_approvals`) and folds an **open card nothing can answer** — the decision row offers buttons that return `Ok(false)` | 1.6 fix-wave re-review | ☑ merged, and it was the one-line mitigation this row named. `forget_minted` drops a drained approval's id from `minted_approvals` at both unanswered drain sites — `RunHandle::drop` and the steer/stop path — so a late `ApprovalRequested` fails `drive_run`'s authority guard instead of folding a card whose buttons can only return `Ok(false)`. **Deliberately not `clear()`**: the session-grant sweep in `respond_approval` ANSWERS what it removes, so those ids must stay minted — their own event may still be in flight and has every right to fold a card the grant then resolves. Both directions are tested |
| D16 | `request_input` still parks its resolver when `engine_tx.send` fails, so a question minted after the run task is gone waits forever — the exact twin of the approval bug 1.6 fixed four lines below it | 1.6 fix-wave re-review | ☑ merged. The input bridge now unparks when `engine_tx.send` fails, exactly as the approval arm four lines below it has since 1.6 — the asymmetry is gone. One difference is deliberate and stated at the site: the question resolves to NO ANSWERS rather than having its resolver dropped, because `interrupt()` and `RunHandle::drop` both send `Vec::new()` for a parked question and a dropped input resolver is a different signal to the consumer. Tested as the twin of `an_approval_the_engine_can_no_longer_show_is_refused_not_parked`, sitting directly above it |
| D34 | **The run path has the relative-executable hazard 2.2 just fixed in the discovery path.** `build_command` sets `current_dir(&request.cwd)` while passing `exe` through unchanged, and `resolve_cli` returns `CLAUDE_CODE_EXECUTABLE` verbatim (`lib.rs:161-163`) — so a relative override plus a session cwd resolves the program against the wrong directory on Unix, per std's documented `current_dir` caveat | 2.2 PR review (#43), adjacent | ☑ merged. Both run launches route through `discovery::program_path` now, which is where the ACP pair already went — the asymmetry was inside one crate. Tested across every provider that sets a `cwd`, not just the two that were broken, so a future adapter that forgets fails the same assertion; the bare-name half is pinned too, so the fix cannot become "absolutize everything" and turn a PATH lookup into a stray `./claude`. **The row's own question is answered and the answer is half-good**: a failed run spawn maps `NotFound` to `NotInstalled(path)`, which reads fine, and every other `io::ErrorKind` to `HarnessError::Io`, whose `Display` is `io: {0}` — a raw OS error on screen through `drive_run`'s sink. That is D130, filed rather than folded in here |
| D130 | **A run spawn that fails for any reason but `NotFound` puts a raw OS error on screen.** `claude/mod.rs` and `codex/mod.rs` both map `spawn()`'s error with the same two arms: `ErrorKind::NotFound` becomes `HarnessError::NotInstalled(path)` — good copy, reaching the models pane as "Agent CLI not found: …" — and everything else becomes `HarnessError::Io(e)`, whose `Display` is `io: {0}`. `drive_run`'s sink renders a `HarnessError` close to as-is, so a `PermissionDenied` (a CLI the account cannot execute) or an `ExecFormatError` (a shim for the wrong architecture) reaches the user as `io: Access is denied. (os error 5)`. Rule 1 of `.agents/rules/user-facing-errors.md`, at a third site after D119 and D100 | answering D34's own "does a failed run spawn surface better" question, 2026-08-30 | ☑ merged, filed and closed the same day. `spawn_failure` reads `io::ErrorKind` — never the error's `Display` — at both run sites: `PermissionDenied` becomes "Not runnable" plus what to check, anything else "Won't start" plus reinstall-or-repoint, and `NotFound` keeps `NotInstalled(path)` unchanged, because that variant's Display is already the product copy the models pane renders. **The variant question resolved to reuse, not a third one**: `hermes::map_open_failure`'s doc sets the test — a new variant earns its place when the user's next ACTION differs, not the wording — and a binary this account cannot execute answers the same "go do one more thing to this agent" as a signed-out one. `NeedsSetup`'s own "built only by map_open_failure" note is widened to say so |
| [D7](D07-remote-allowlist-partition.md) | The remote allow/deny partition in `remote_access.rs` is exhaustive only by two hand-maintained literal lists; a new method omitted from both silently defaults to denied with no test failure | 0b.2 Task 8 review | ☑ merged. `every_declared_rpc_method_is_allowed_or_denied_on_purpose` reads every method constant out of `comet_rpc`'s source and fails when one lands on neither side — plus three more directions the hand lists could go wrong: a method on BOTH sides, a listed method the crate no longer declares, and a list that disagrees with what `remote_method_allowed` answers. **The partition turned out to be complete** (54 declared, 17 denied, 37 allowed), so this bought no fix and the whole guarantee: the next method added to neither list now fails instead of defaulting to denied in silence, which is how `ListHarnessDiagnostics` got in. Source-scanned rather than a `methods::ALL` array, which would be the thing nobody updates |
| D51 | **A store-level identity failure paints a raw technical string on the Retry card**: `DACL: identity I/O: C:\...\device-identity.pem does not have the private...`, truncated mid-sentence. Reproduced by copying a `COMET_DATA_DIR` to a new path, which breaks the key file's restrictive ACL. Rule 1 of `.agents/rules/user-facing-errors.md` — no raw error on screen — with no `summary`/`hint` split and no action beyond Retry, which cannot succeed | 4.1 rendered check, 2026-08-12 | ☑ merged, and it was **already fixed** — this change is the record and the pin, not the repair. The row was filed against slice 4.1 (2026-08-12); [PR #98](https://github.com/matty/comet/pull/98) added `errors::engine_start_failure` afterwards and translated every non-`AlreadyRunning` assembly failure to "Comet's engine couldn't start.", which is the arm an identity failure takes (`EngineError::Identity`). Nobody went back to update this row, so it read open for two and a half weeks after the fix. `a_broken_identity_file_does_not_put_its_path_on_the_gate` now pins D51's exact chain — the `PermissionDenied` on `device-identity.pem`, wrapped in the same `DACL` anyhow context the real failure carried — and asserts the path, the context and the `identity I/O` prefix all stay off the gate. Verified by breaking it: with the catch-all returning `format!("{err:#}")` the test fails |
| [D9](D09-unparseable-sentinel.md) | The `unparseable` sentinel merges "garbage on stdout" with "a claimed frame whose schema changed" — the second is the likelier drift and the information to distinguish them is already in hand | 0b.2 whole-branch review | ☑ merged, both sides. Claude's `parse_frame` returns `FrameParseError { kind, error }` — `None` for a line that was never JSON (the bare sentinel stays), `Some(type)` when only the TYPED decode failed, so a `result` frame whose shape moved reads `unparseable/result` and an operator can tell it from log noise on stdout. Codex and ACP share `Incoming::Malformed(MalformedKind)`, which carries the distinction the reader always had and threw away: `NotJson` keeps the bare sentinel, `NotAnObject` and `NotAMessage` name themselves. **The page's premise needed one correction**: an unrecognized `type` never reaches the parse-failure path — it becomes `Frame::Unknown` via `classify_unclaimed` — so the `unparseable/*` vocabulary is a closed set and needs no cap, which a test now records |
| [D10](D10-unbounded-logging.md) | The registry is bounded; the logging and journaling behind it are not. A renamed high-volume method moves from Ignored to Unknown and warn-logs its full payload indefinitely | 0b.2 whole-branch review | ☑ merged, by the mitigation the page names. `log_budget` gives each discriminator five full-fidelity logs and a rising count thereafter, at every drop site — Claude's `classify_unclaimed` and unparseable arm, plus the three JSON-RPC reader arms Codex and both ACP agents share. The key table caps at 64, matching the registry's cap deliberately, and a discriminator past the cap still LOGS, just never with its payload: losing the payload is the point, losing the fact would hide the drift. **The close is narrower than the row's title** — the journal append and broadcast per diagnostic are untouched; the registry bounds their distinct count but nothing samples them |
| [D8](D08-claude-inner-diagnostics.md) | Claude has no **inner** diagnostic sink — unknown `stream_event` delta kinds and unknown content-block kinds are dropped silently inside frames classified as fully Claimed | 0b.2 whole-branch review | ☑ merged. Claude has the inner sink Codex got in 0b.2: `INNER_IGNORED` names the structural framing, `ping`, the opaque `signature_delta` and the buffered `text`/`thinking` blocks whose streamed halves are already claimed; everything else reports as `delta/<kind>`, `stream_event/<kind>` or `block/<kind>`. **Once per RUN, not per frame** — the thing the page's fix shape did not say and the implementation had to: these arrive inside streamed deltas, so an unknown delta kind on a long tool input would journal one event per chunk. Capped at 32 distinct kinds, the key being provider-chosen |
| D111 | **A rotated `assistant_message_id` is never announced through `Steered`.** `Steered`'s own `next_assistant_message_id` field is hardcoded `None` at every emit site in `crates/harness/src/acp/session.rs` (`:966-967`), pre-existing and untouched by PR4 | PR4 task report (`task-4-report.md`), 2026-08-28 | ☑ merged. Both halves are named now: `assistant_message_id` is the id the interrupted turn's output actually folded into — captured BEFORE the turn boundary rotates it, which is the ordering subtlety that made this look bigger than it is — and `next_assistant_message_id` is the one the steered answer will use. **A turn that streamed nothing never reached the boundary rotation**, so the steer arm rotates there instead; `Steered`'s contract is that post-steer output folds into a FRESH message, and a consumer handed the same id twice could not separate the two segments. The existing steer test now asserts both ids are present, differ, and that the interrupted one matches a message actually completed rather than a value invented at the steer |
| D4 | Thirteen mutation sites still `format!("{err}")` an `RpcError` onto the screen | 0.2b | ☑ merged, and **the count was wrong**: thirteen was recorded, eleven was measured — seven bare `format!("{err}")` and four behind a hand-written prefix (`"Stop failed: {err}"`), which the shape the inventory grepped for would have missed. All eleven now go through `errors::mutation_failure`, which takes a `Mutating` enum beside the existing `Loading` — one place for the transport and version-mismatch arms, and a per-action remedy for the arm where the engine ANSWERED and refused, because "try again" is wrong advice for half of them. A test asserts every arm against every `RpcError` variant (not a sample) and that the refusals do not collapse to one string. It also caught the continuation-space trap in the new copy before it shipped — the fourth appearance of that trap in this codebase |
| D97 | **Codex add-account still opens two authorization tabs on Windows.** #103 suppressed the CLI's own open by pointing it at a no-op `BROWSER` script, but that is `#[cfg(unix)]` on both sides (`crates/engine/src/agent_accounts.rs:576`, `ensure_noop_browser` at :1617) and cannot just be widened: the `webbrowser` crate the CLI uses only consults `$BROWSER` on unix and reaches the platform opener directly elsewhere, so there is no env var to hand it. Windows therefore keeps the original double-tab bug — the CLI opens one, the app opens another when the start reply lands | PR #103's port of upstream `bca16a35d`; stated in the comment correction on [PR #106](https://github.com/matty/comet/pull/106) and never given a row, 2026-08-25 | ☑ merged, and **the confirmation this row demanded was done first**: `codex login` under a temp `CODEX_HOME` on Windows 2026-08-30 raised the browser process count by one and printed "If your browser did not open, navigate to this URL" — so the CLI does open its own. `AgentLoginStart` now carries `cliOpenedBrowser`, the engine answers it from its own platform (`cfg!(not(unix))`, the mirror of the `BROWSER` no-op), and the client skips its automatic open only when the CLI already made one **on the same machine**. That last clause is the part a naive fix would miss: a remote engine's tab opens on the remote desktop and is no use here, so a login aimed at another device still opens locally. Additive with `false` as the absent value — an older engine keeps today's double tab, which beats a user with no tab at all, and a test pins that direction. The "Reopen the…" link is untouched either way |
| D62 | **`--resume-id` is operator-supplied and unvalidated against the scenario that produced it.** Both `resume` and `checklist-resume` take a raw session id with nothing recording which capture it came from; pointing `checklist-resume` at a `fresh-text` session is accepted, spawns the provider, and spends tokens. The id also has to be regexed out of the previous run's `capture.json` by hand | found running the checklist pair, 4.3 | ☑ merged, by the cheapest fix this row names. It was **worse after the neutral-recorder stage than the row first said**: it previously named the D61 mutation guard as the thing a mismatch eventually failed on. That guard is deleted (D61, closed two rows below), so today a mismatched `--resume-id` no longer fails at all — it spawns the provider, spends tokens, and produces a **successful capture mislabeled as a resume of a session it never touched**. That is what landed: `Session::finish` writes the session it opened to `session-id.txt` beside `capture.json`, and `--resume-id` accepts that directory (or the file) as well as a bare id, so the pairing is mechanical. The extractor reads the union of the providers' spellings — `session_id`, `sessionId`, `threadId` — in one short function rather than a fourth `CaptureProvider` member, which AGENTS.md is explicit about after spawn had to move back off that trait. Best-effort by design: a discovery capture opens no session, an empty id is not recorded, and neither fails a capture that already succeeded. **A bare id still works**, because a capture recorded before this has no file to point at. What this does NOT add is validation that the id came from a COMPATIBLE scenario — the row's second clause — which needs the resumed capture's provenance record it also calls a separate mechanism A resumed capture's provenance record is a different mechanism than this stage's recorder rewrite and stays out of its scope |
| [D52](D52-ui-state-previews.md) | **There is no way to see a UI state without producing it for real.** Reviewing the 4.1 gauge at four fills needed a filled context window; the ad-hoc escape was a one-off `COMET_CONTEXT_DEMO` env knob, and the attempt before it spent three real Fable turns driving the app into the state | 4.1 rendered check, 2026-08-12 | ☑ **merged by slice 4.4** ([PR #115](https://github.com/matty/comet/pull/115)). The answer is the page's **option 3**: the `COMET_MOCK_*` knobs are the mechanism, formalized as a family with a register (`docs/testing/mock-states.md`) and a two-directional test (`every_mock_knob_is_documented`) that fails on an undocumented knob and on a stale row alike. Chosen over the gallery route because these are *data-side* — they script provider events that run through the real fold, document and components, so two of the page's three constraints hold by construction. **The limit is now written down rather than rediscovered:** a knob scripts a provider, so any state a provider is not the source of (a steer boundary, an engine sweep) is out of reach — see that register's "States no knob reaches" |
| D53 | ~~**The subagent surface itself is not built.**~~ The events and the persisted part landed in 4.2 (tasks 1–7) while `transcript.rs`'s render loop kept an explicit `MessagePart::Subagent { .. } => {}` no-op arm — persisted, never drawn, by the arm's own comment. The arm was explicit rather than a wildcard, so a `MessagePart` kind added beside `Subagent` still failed to compile until handled | 4.2, deliberate — no UI ships this slice | ☑ **merged by slice 4.4** ([PR #115](https://github.com/matty/comet/pull/115), `87bab035`). `RowKind::SubagentCard` draws it; `transcript.rs`'s no-op arm is gone. **The row is kept rather than deleted because its mechanism still stops a misreading:** the card implements **two independent** activity-blanking rules and neither subsumes the other — a terminal STATUS blanks the activity line (this row: `doc/src/parts.rs`'s `SubagentUpdated` fold overwrites `activity` only when the new reading reports one, so a `task_updated`/`task_notification` carrying no `activity` field leaves the last live line standing after the card goes terminal), and a finished ENTRY blanks it even while the status still reads `Running` (D57). Pinned by `a_completed_subagent_still_carrying_activity_draws_no_activity_line`, whose fixture sets `activity: Some(..)` alongside a terminal status deliberately — one that cleared it could not fail. Cited from `crates/ui/src/transcript.rs:836` and `crates/harness/src/mock.rs:623` |
| D59 | ~~**A terminal `Subagent` card can be reopened by a status the terminal guard doesn't recognize as terminal.**~~ Route A: `task_updated`/`task_notification`/the `tool_use_result` fallback carrying a status string `subagent_status` doesn't recognize (a future CLI spelling — "succeeded", "timed_out", …) degraded unconditionally to `Running` even after a stored terminal reading, permanently reopening a finished card. Route B: `task_progress` (`normalize_subagent_task`'s `"task_progress"` arm) assigned `status: SubagentStatus::Running` UNCONDITIONALLY, with no read of any prior state, so a tick arriving after a terminal reading reopened the card the same way | 4.2 fix-wave review | ☑ merged. Route A closed in the 4.2 fix wave; **Route B closed 2026-08-30 ([PR #178](https://github.com/matty/comet/pull/178))** — the `task_progress` arm now goes through `status_or_carry_forward` (`crates/harness/src/claude/normalize.rs`) like its siblings, so a stored terminal status carries forward instead of being overwritten. `SubagentStatus` has exactly one non-terminal variant, so carrying forward can only ever yield the stored terminal reading or `Running` — the no-prior-state case is unchanged. Never observed live on either route: no capture ever showed a `task_progress` after a terminal reading, but nothing on the wire ruled it out and Route A's identical mechanism was real. Pinned by `task_progress_after_a_terminal_reading_does_not_reopen_the_card` |
| D114 | ~~**The foreign-session half of the turn-completion guard ships unexercised.**~~ `crates/harness/src/acp/session.rs`'s completion-notification arm is guarded on the session matching ours (a "foreign session" is not evidence about this turn) before the promptId staleness check — real, load-bearing logic per Grok's own hardening notes (`.github/upstream-sync.json`'s `2133bae5a2` note), and no committed test drove a foreign `sessionId` down this path | PR5 hardening slice, found in PR10 closeout review, 2026-08-29 | ☑ merged 2026-08-30 ([PR #179](https://github.com/matty/comet/pull/179)), test-only; production unchanged. `crates/harness/tests/acp_turn.rs`'s `a_foreign_session_completion_notification_is_not_evidence_about_this_turn` drives a foreign `sessionId` down the arm via the fixture's new `complete-foreign-session` scenario. Falsified by disabling the sessionId comparison: the turn settled `Errored` on the foreign notification's own `stopReason` instead of `Completed`, which is what proves the guard load-bearing. **The row's own premise was wrong and that is the durable part:** it said the fixture "would need a second, concurrent fake session". It does not — `incoming` is a plain per-process channel and the guard is a string comparison, so one fixture emitting a notification with a mismatching `sessionId` string reaches the same path |
| D72 | ~~**Codex says which model is its default and Comet does not read it.**~~ `model/list` carries `isDefault` per row (`gpt-5.6-sol: true` in the promoted catalog reply); the name appeared in `crates/` only in two test fixtures. `pickers::default_model` returns `models.first()`, so the default was **catalog order**, agreeing with the server only by coincidence — the curated Codex catalog happens to lead with the same flagship | C0 surface map, 2026-08-14 | ☑ merged 2026-08-30 ([PR #182](https://github.com/matty/comet/pull/182)), by the cheap route this row priced: `codex::catalog::order_by_live_default` promotes the live reply's `isDefault` row to the front of the merged catalog before `models.first()` ever sees it, so **nothing on the wire changed and `PROTOCOL_VERSION` is untouched** — no field was added to `comet_proto::Model`. The default rides from `page_from_reply` to `models()` on a side channel (`CodexHarness::live_default_model`) rather than on `DiscoveredModel`, which `claude/discovery.rs` also builds and which has no `isDefault` to report. **Three absent cases are each a deliberate no-op, not a fallback to "first row wins":** no discovery ran, no row claimed the default, or the claimed id names no row in the merged catalog (a hidden model can still be the reported default, so the read happens before the `hidden` filter). `clear_discovery` forgets it, so a retry cannot inherit a stale pick. Pinned against the literal captured reply, plus a hand-written one where `isDefault` names a row that is neither first nor the curated flagship — the case a decoder checking `data[0]` would pass every other test and still get wrong |
| D17 | ~~A degenerate `Edit` (empty `new_string`, absent `old_string`) maps to `FileChange{Modify, +0, −0}`, which **is** allowlistable, and its signature is identical to a real edit to that path~~ — so allowing that card for the session auto-allowed every subsequent real edit to the same file | 1.6 fix-wave re-review | ☑ merged 2026-08-30 ([PR #184](https://github.com/matty/comet/pull/184)), in the **adapter**, not the engine: only `claude/approval.rs` can see whether `old_string` was ever supplied, so only it can tell a degenerate edit from a genuine Write-truncate-to-empty, which renders the same `+0 −0` and is D18's business. One arm — an empty `new_string` with an absent-or-empty `old_string` now decodes as `FileOperation::Unknown`, inheriting the `Unknown => None` non-allowlistable treatment `approvals.rs` already gives its sibling. **The degenerate shape is still uncaptured**: the fix reasons from `claude/2.1.251/edit/` (an ordinary edit) plus the tool's required-`old_string` contract, not from a recorded degenerate frame, and the PR says so. **The sibling door is open and is now D132** — do not read this row as closing the whole class **Postscript, 2026-08-30 ([PR #190](https://github.com/matty/comet/pull/190)): the fix is confirmed DEFENSIVE-ONLY.** `claude/2.1.251/edit-noop-approval/` records the degenerate shape under a permission-required run and contains **zero** `can_use_tool` frames: Claude's own schema validation rejects it (`InputValidationError: The required parameter old_string is missing`) before, or instead of, the permission hook, so it never reaches Comet's approval path from a live CLI. Keep the arm — it costs nothing and a future CLI could reorder those steps — but do not cite this row as evidence a user could ever have hit it. **One limit worth carrying (D74):** `new_string` sanitizes to a placeholder, so the archive cannot independently show it was empty; the conclusion rests on the validation-error text, not on reading the redacted value. |
| D42 | ~~**The composer's resting height changes with the space.**~~ `Pickers::render_footer` returned `None` unless a space row existed *and* `space.git_detected`, so a git space got a branch/worktree toolbar under the pill and a non-git one did not — **36px** of difference (20px chips, 8px above from the container gap, 8px below carved out of its bottom padding with `mb(-8)`). Added by `47f8ba6` (2026-07-23, the t3code BranchToolbar port) | noticed 2026-08-11 while checking whether slice 2.4 had changed composer geometry; it had not | ☑ merged 2026-08-30 ([PR #185](https://github.com/matty/comet/pull/185)). `render_footer` returns `AnyElement`, not `Option<AnyElement>`, and both branches build the row through one no-argument `footer_row()`, so the full 36px is reserved whether or not the space is git-detected — the same reservation `Theme::STATUS_STRIP_HEIGHT` makes for the status strip, and the height a git space always had is unchanged. **The guarantee is type-level, and the PR says why:** `crates/ui` has zero `#[gpui::test]`, so nothing in the crate can drive a render function. A first attempt shipped a helper whose test compared a constant to itself — green against the real bug reintroduced — and was replaced rather than explained; the falsification that stands is that restoring `return None` no longer compiles |
| D88 | ~~**`instance_lock::tests::holder_probe_reports_pid_without_disturbing_the_lock` is flaky on Linux CI, and nothing recorded it.**~~ The test acquires a lock, probes the holder, drops the lock, then asserts `InstanceLock::holder(dir)` is `None`; on PR #72's first CI run it returned `Some("5007")`. **The flake named a production defect:** `holder()` read whatever pid was on disk the moment it failed to win the exclusivity check, with nothing confirming that pid named a running process | PR #72 CI, 2026-08-16 | ☑ merged 2026-08-30 ([PR #186](https://github.com/matty/comet/pull/186)), by fixing `holder()` rather than retrying the assertion — the row's own preferred close. Two mechanisms, one per platform: **Unix**, `flock` can return `EINTR` and `holder()` did not retry it though `acquire()` two lines away does, so a signal read as contention; **Windows**, reproduced deterministically, *any* unrelated handle on `engine.lock` (an indexer, a scanner) trips the identical sharing-violation path a real second engine would. `pid_is_alive` now confirms the pid first. **The caller needed fixing too:** `comet status` returned early only on `Some`, so an unverifiable lock fell through to `acquire_offline_lock` and printed a raw error *asserting the engine was running* — it now says `Engine: unknown (lock file is in use, but no running engine could be confirmed)` with the diagnostic in `tracing`. **Residual, deliberate: pid reuse.** `pid_is_alive` confirms *a* live process with that pid, never that it is the same one that stamped it — acceptable only because `holder()` gates nothing; `acquire()` is the real exclusivity guard and never consults the stamp |
| D98 | ~~**A late `SubagentUpdated` cannot reach the segment its card is in, so a background agent's answer and usage are lost.**~~ Claude's `Agent` tool is not synchronous with its parent's turn: a captured 2.1.246 run ended the turn `Completed` (journal seq 57, `result: "Agent is running. Waiting for completion notification."`) and the child reported `completed` with its summary and full usage at seq 61-62, by which time the segment was finished and the accumulator cleared | 4.4 rendered check against a real CLI, 2026-08-26 | ☑ merged 2026-08-30 ([PR #187](https://github.com/matty/comet/pull/187)). `drive_run` keeps a `task_id → entry_id` map scoped to the RUN and routes an update the live fold no longer holds into the finished entry via `SessionDoc::apply_late_subagent_update`, which patches partially — `status` always, every other field only when `Some`, so an absent `activity` never clears a prior reading. **The run is the bound, and deliberately not a timer:** the run's process is the only thing that can still hear from the child, so a wall-clock number would be a figure nobody measured. **It exposed a second, latent bug:** once a persistent session parked after `Done`, any event but `Notice`/`SessionTitled` read as the next turn beginning — clearing `idle_since`, disarming the idle reaper and synthesizing a spurious errored entry when the stream closed. `SubagentStarted`/`SubagentUpdated` now carry the same "can arrive outside a turn" exemption. D57's `last seen running` is intact: all four subagent e2e scenarios (steer, clean completion, cut-short, late completion) pass together |
| D122 | ~~**Grok's completion notification is recognized inside the provider-neutral turn loop, against that file's own doctrine.**~~ The method name, the settle arm's guard, `params["promptId"]`, `result["_meta"]["promptId"]` and the post-notification reply bound were all `_x.ai` facts living in `crates/harness/src/acp/session.rs`, which injects three seams for exactly this reason | `/simplify` altitude review, 2026-08-29 (finding 2), deferred by [PR #138](https://github.com/matty/comet/pull/138) | ☑ merged 2026-08-30 ([PR #191](https://github.com/matty/comet/pull/191)). All four moved behind a fourth injected seam, `SettleSignal`, supplied by `acp/grok.rs` and passed as `None` by `acp/hermes.rs`. **`PROMPT_COMPLETE_METHOD`'s own doc argument is preserved, not overridden:** the shared arm still recognizes a notification by comparing its wire `method` against injected data, never by matching `HarnessId`, so it cannot go silently dead for a third agent that grows its own version under a different name — Hermes' inertness falls out of supplying `None`, and nothing in `session.rs` names either agent. **Falsified one fact at a time, four separate rounds**, because breaking two lets them mask each other: the method (the turn never settles), the sessionId guard (D114's foreign-session test flips to `Errored`), both promptId paths (the exactly-one-`Done` test), and the reply bound (usage harvest drops to `0`) — that last one is what proves #136's usage harvest still runs through the seam, the thing a refactor could most easily have dropped in silence. `crates/capture`'s calls into `acp::grok::run_launch` and `acp::prompt_params` keep their signatures |
| D103 | ~~**`SpacesSync` has no way to synchronously stop watching a space's folder.**~~ `notify` 7.0's `ReadDirectoryChangesWatcher` `Drop`/`unwatch` only post a stop request to a background thread whose `JoinHandle` is discarded at spawn, so nothing built on `crates/engine/src/spaces.rs` could confirm a watcher had released its directory handle before proceeding | found diagnosing D101, PR #128, 2026-08-29 — the reason a teardown-ordering fix for D101 was rejected as unsound rather than merely deprioritized | ☑ merged 2026-08-30 ([PR #192](https://github.com/matty/comet/pull/192)), by the row's second option — Comet's own fence, no upstream patch, no sleep, no polling. `SpacesSync::stop_watch(space_id, timeout)` queues `unwatch()` then `configure()` on the watcher's single-consumer action channel; `configure()` blocks on a `bounded(1)` ack (`notify-7.0.0/src/windows.rs:524`) and `Action::Unwatch` runs notify's own `stop_watch`, which `CancelIo`s and then waits on the completion semaphore, so the ack can only arrive after the handle is released. **A timeout returns `false` meaning "not confirmed yet", never "stop failed"** — the request is already queued and the watcher finishes dropping regardless, so a caller needing certainty treats `false` as "cannot proceed", never as a reason to retry unboundedly. **The test does not reproduce D101 and says so:** a plain drop-then-remove probe reproduced it 0/50, because Windows tolerates deleting a path whose only handle sets `FILE_SHARE_DELETE` — which `notify`'s `CreateFileW` and Rust's own `File::create` both do. It proves the structural claim instead, with a handler holding a handle that excludes `FILE_SHARE_DELETE`. `#[cfg(windows)]` on purpose: Linux's `inotify` backend already blocks inside `unwatch()`, so the hazard is Windows-only and **CI can never gate this**. Nothing in production calls it yet — `EngineCore::shutdown()` wiring, and retiring D101's `unwatched_space_root` workaround, are named follow-ups, not part of this row |
| [D46](D46-provider-process-tree-cleanup.md) | ~~**Cancellation tests prove the direct fake exits, not that a provider-owned process tree is gone.**~~ The fixtures spawned no descendants, while both platform helpers signalled or terminated only the direct pid — so a cancelled session could look complete in Comet while a provider-owned shell, safety helper or MCP server kept running | fake-provider review, 2026-08-11 | ☑ merged 2026-08-30 ([PR #197](https://github.com/matty/comet/pull/197)) for the Claude and Codex adapters, on both shipped platforms. **The policy was decided before any termination code changed, as the page required:** reach the tree through an OS-native grouping established **at spawn**, never by scanning for processes afterwards. Unix spawns into its own process group and signals it via `killpg`; Windows assigns the child to a Job Object with `KILL_ON_JOB_CLOSE`, held in the `Session` so its lifetime is the session's, and escalates with `TerminateJobObject`. **Deliberately out of reach:** a process that breaks out of its group or job on its own — catching those needs the system-wide heuristic scan the page warns against. **Two residuals, both stated rather than hidden:** the Windows spawn-to-assign window is not atomic (closing it means `CREATE_SUSPENDED` and resuming by hand, a bigger change than this row asked for), and the ACP adapter shares the unmodified `shutdown_child`, so it gets Unix's fix free and Windows' not at all — **that half is now D133**. Coverage is split across the two of us on purpose: the new test is not platform-gated, so CI's Ubuntu run exercises the `killpg` path against a real grandchild while Windows was run by hand — neither platform is claimed on the other's evidence |
| [D14](D14-approval-card-states.md) | ~~The card and the composer decision row show the **same label and detail line**; `Allowed` / `Denied` / `Allowed for this session` **paint identically**~~ — both fixed in [PR #36](https://github.com/matty/comet/pull/36) (`5a8a16e`). **Residual: the deny note is written to the model and then vanishes from the UI** | 1.5 rendered check | ☑ **three of three closed.** The first two in [PR #36](https://github.com/matty/comet/pull/36) (`5a8a16e`); the residual — the deny note written to the model then vanishing from the UI — in [PR #195](https://github.com/matty/comet/pull/195), as a `RowKind::DenyNote` row of its own rather than a field on the card. **The card was not grown on purpose:** its height is fixed across every decision so a decision landing cannot reflow the transcript under the user's scroll position, and a note has no length bound, so putting it on the card would break the invariant the card exists to protect. The row shows what was actually sent to the model, including the composer's default text when the user typed nothing |
| D18 | ~~A `Write` that overwrites an existing file carries the right verb ("Edit a file") but still renders `+N −0`, so the card understates the loss~~ — it names what arrives and not what is replaced | 1.6 PR #37 review | ☑ merged 2026-08-30 ([PR #196](https://github.com/matty/comet/pull/196)), and it is **three facts, not one**. **(1)** The transcript's post-execution diff was already correct: `complete_tool_diff` (`claude/normalize.rs`) has read `tool_use_result.originalFile` for a `WriteFile` overwrite since PR #68 — it had simply never been exercised for that shape, and now is, against `claude/2.1.251/write-overwrite`'s literal frames. No production code changed. **(2)** The **approval card** cannot get the replaced count from `tool_use_result` at all: `approval_request` runs at `can_use_tool` time, and `edit-create-approval`'s frame order (45 request → 46 Comet's allow → 50 result) proves the data does not exist yet. That is the new fact. **(3)** The card could still be corrected another way — by reading the file at approval time, which `approval_request`'s injected `file_exists` callback already does synchronously for the Create/Modify decision. **That is exactly the cost this row originally named and declined, and it is neither cheaper nor ruled out** — do not read (2) as closing that door |
| D28 | ~~Recovery from an expired turn is manual: the note tells the user to send again, and there is no button that does it~~ — the prompt sat in the transcript with nothing wired to resubmit it | 1.9 spec §8, deliberate | ☑ merged 2026-08-30 ([PR #195](https://github.com/matty/comet/pull/195)), alongside D14's residual exactly as this row predicted. `approvals::expired_resubmit_prompt` is pure: it offers the nearest preceding user message only when the LAST assistant entry is terminal and carries an unattended-expiry note, and the composer calls the existing send path — no new engine state, no RPC, no `PROTOCOL_VERSION` question. **The expiry is recognized by a shared constant, not a copied string:** `comet_engine::RESUME_CLAUSE` is defined once, interpolated by `unattended_note`'s `format!`, and compared by the UI, so the two cannot drift — a first pass kept two hand-typed copies in step with a test, which is weaker than the compiler. **Residual, deliberate:** the expiry is still recognized by prose rather than a structured wire fact; making it structural is a `PROTOCOL_VERSION` question nobody has asked for |
| D132 | ~~**The `Edit` allowlist hole has a second door, and what the input means is unsettled.**~~ `approval_signature` builds `["fileChange", path, operation]` and **drops the line counts on purpose**, so every `Modify` on one path shared one signature. An `Edit` with a non-empty `new_string` and no `old_string` decoded as `Modify`, so approving that card for the session silently allowed every real edit to the same file | found reviewing D17's fix, [PR #184](https://github.com/matty/comet/pull/184), 2026-08-30 | ☑ merged 2026-08-30 ([PR #196](https://github.com/matty/comet/pull/196)). That shape now decodes as `FileOperation::Create` in `claude/approval.rs`, earning a signature distinct from `Modify` on the same path. **The decision came from evidence, not inference** — `claude/2.1.251/edit-create/` shows the CLI creating the file, which is why the answer is `Create` and not `Unknown`, and `edit-create-approval/` shows `can_use_tool` firing for it, which is why the fix is load-bearing rather than defensive (contrast D17, whose shape never reaches the hook at all). **Arm order matters and is load-bearing:** D17's `text.is_empty()` guard sits above this arm, so the defensive case is not swallowed. No filesystem check — no capture exists for this shape against an *existing* path, so nothing here guesses what that would mean |
