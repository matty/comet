# Carried-forward debt

Known-open work that is **not** on a phase plan, because it was discovered
mid-slice and deliberately deferred rather than planned. This file is the index:
one row per item, scannable in one pass. An item whose *reasoning* matters gets
its own page beside this one, linked from its row.

Read this before starting a slice — several rows name the slice that owns them.

## How to keep it

- **Add a row when a slice defers something.** The row is the minimum: what it
  is, which review or capture found it, and what state it is in.
- **Write a page when the reasoning outlives the row.** A ruling ("this is a
  decision, not an oversight"), a corrected premise, or a mechanism someone will
  otherwise re-derive. A page is `D<nn>-<slug>.md`, and it is what a PR comment
  or a code comment links to.
- **Delete a row when it merges** — except where the *explanation* still stops
  someone reading the code wrong. Those move to [`closed.md`](closed.md), and
  the row stays with a `☑ merged` state and its PR.
- **Numbers are never reused.** A deleted D-number stays deleted; rows and slice
  logs elsewhere refer to them by number.

Legend: ☐ open · ◐ partly closed · ☑ merged

## Open

| # | Item | Origin | State |
| --- | --- | --- | --- |
| [D3](D03-unavailable-agent-selection.md) | ~~An unavailable agent can still be the selected one~~ — **not a defect**. Residual: no *pre-send* signal in a locked session | 0.2a | ☐ low priority, optional polish |
| D4 | Thirteen mutation sites still `format!("{err}")` an `RpcError` onto the screen | 0.2b | ☐ deferred, **count unchanged by 1.5** — the slice added `crates/ui/src/errors.rs:approval_failure` rather than a fourteenth site. Inventory in `.agents/rules/user-facing-errors.md` under "Known debt" |
| [D7](D07-remote-allowlist-partition.md) | The remote allow/deny partition in `remote_access.rs` is exhaustive only by two hand-maintained literal lists; a new method omitted from both silently defaults to denied with no test failure | 0b.2 Task 8 review | ☐ open, **unowned** |
| [D8](D08-claude-inner-diagnostics.md) | Claude has no **inner** diagnostic sink — unknown `stream_event` delta kinds and unknown content-block kinds are dropped silently inside frames classified as fully Claimed | 0b.2 whole-branch review | ☐ open — plan gap, Codex got sink 4 and Claude did not |
| [D9](D09-unparseable-sentinel.md) | The `unparseable` sentinel merges "garbage on stdout" with "a claimed frame whose schema changed" — the second is the likelier drift and the information to distinguish them is already in hand | 0b.2 whole-branch review | ☐ open |
| [D10](D10-unbounded-logging.md) | The registry is bounded; the logging and journaling behind it are not. A renamed high-volume method moves from Ignored to Unknown and warn-logs its full payload indefinitely | 0b.2 whole-branch review | ☐ open |
| D13 | Codex declares `auto-accept-edits`, but the linked-worktree sandbox workaround silently raises that mode's workspace-write sandbox to `danger-full-access` — trigger is any linked worktree on a slash-named branch, i.e. this repo's own `feature/…` convention — with only a `tracing::warn!` as the signal | 1.3 whole-branch review | ☐ open — 1.3 documents the exception in the capabilities comment; making it visible to the user needs a notice per `user-facing-errors.md`. **1.5 did not claim it and neither did 1.8** — 1.8 made the mode *selectable*, which makes this reachable by a user rather than only by the default, so it is now **unowned and more visible than when it was filed** |
| [D14](D14-approval-card-states.md) | ~~The card and the composer decision row show the **same label and detail line**; `Allowed` / `Denied` / `Allowed for this session` **paint identically**~~ — both fixed in [PR #36](https://github.com/matty/comet/pull/36) (`5a8a16e`). **Residual: the deny note is written to the model and then vanishes from the UI** | 1.5 rendered check | ◐ two of three closed, **unowned** |
| D15 | An approval drained at a steer or by a session-grant sweep can be outrun by its own `ApprovalRequested`: the event still passes the authority guard (its id is in `minted_approvals`) and folds an **open card nothing can answer** — the decision row offers buttons that return `Ok(false)` | 1.6 fix-wave re-review | ☐ open. Bounded — the next steer or `Done` stamps it `Expired`. One-line mitigation: also remove drained ids from `minted_approvals` |
| D16 | `request_input` still parks its resolver when `engine_tx.send` fails, so a question minted after the run task is gone waits forever — the exact twin of the approval bug 1.6 fixed four lines below it | 1.6 fix-wave re-review | ☐ open, and **visibly asymmetric**: 1.6 made the approval bridge fail closed and drains `pending_inputs` in `RunHandle::drop`, but left the input bridge alone. Two lines |
| D17 | A degenerate `Edit` (empty `new_string`, absent `old_string`) maps to `FileChange{Modify, +0, −0}`, which **is** allowlistable, and its signature is identical to a real edit to that path — so allowing that card for the session auto-allows every subsequent real edit to the same file | 1.6 fix-wave re-review | ☐ open. Same hole closed in the same wave (`FileOperation::Unknown` is no longer allowlistable), entered by a different door. Degenerate input, low likelihood |
| D18 | A `Write` that overwrites an existing file carries the right verb ("Edit a file") but still renders `+N −0`, so the card understates the loss — it names what arrives and not what is replaced | 1.6 PR #37 review | ☐ open. Correcting it means reading an arbitrary-size file on the frame loop to count the replaced lines, a different decision from the verb fix (`9029329`) |
| D19 | `ApprovalRequest::Mcp` carries **no arguments**, so an MCP approval cannot be identified precisely enough to allow again — 1.6 made it un-allowlistable, so "Allow for this session" silently degrades to a plain Allow on MCP cards. The card is equally blind: `server · tool` and no arguments | 1.6 PR #37 review | ☐ open, **owner is 1.7 or later**. Needs the proto variant to carry a discriminating digest of the arguments, which is a `PROTOCOL_VERSION` question 1.6 froze. Fixing the signature and the card are the same change |
| D20 | Codex's `acceptForSession` **works** and is deliberately unused: 1.7 sends `accept` on both allow arms so Comet's engine stays the single grant authority. The server's grant is keyed on **path alone** and spans the operation kind, while `approval_signature` keys on path **and** operation — delegating would leave two caches with different scopes | 1.7 capture, runs 5–6 | ☐ open **by decision, not by omission**. Revisit only if the two-cache divergence is judged acceptable; it needs a provider-specific carve-out in `engine/src/approvals.rs`, the one function 1.6 built to be provider-agnostic |
| D21 | `cancel` — Codex's "deny **and** interrupt the turn" decision — has no button. 1.5's decision triple maps Deny to `decline`, which lets the agent continue; nothing expresses the interrupting variant except Stop, a separate gesture the user has to know to reach for | 1.7 capture + generated schema | ☐ open, **unowned**. Untested live. **1.9 shipped as the unattended-policy slice and did not touch this** — the "candidate owner is 1.9" note was speculative and is stale |
| D22 | `item/permissions/requestApproval` — a third Codex approval method (filesystem/network permission overlays) that fits no `ApprovalRequest` variant. 1.7 answers it as `Unknown` written **blind**: no capture run produced one | 1.7 capture / `ServerRequest` schema | ☐ open. Capture the first live one and revisit the mapping; until then the card says "Grant Codex additional permissions" and the grant is un-allowlistable |
| D23 | `availableDecisions` arrives on every command approval, is **absent from the generated schema**, and lists only the decisions the CLI's own picker would show (`accept`, `acceptWithExecpolicyAmendment`, `cancel`) — never `decline`, which nonetheless works. Comet ignores the field | 1.7 capture, runs 1/3/4 | ☐ open, low priority. It becomes a signal worth reading if a future CLI starts *enforcing* it, at which point Deny would silently stop working with no error |
| [D25](D25-composer-picker-rework.md) | **The chat/composer surface and the picker menu need a rework, not another feature bolted on.** Every permission, approval and traits control added in Phase 1 landed inside a composer and a merged model+traits popover designed for neither | 1.8 rendered check, user ruling | ☐ open, **accepted deliberately**. Not a blocker for 1.8 or for Phase 1; the next slice that wants to add another control to either surface should stop and cost the rework |
| [D26](D26-done-status-no-expiry-variant.md) | `DoneStatus` has no variant for an unattended expiry — `expire_unattended` ends the turn through the same `interrupt()` a user's Stop calls, so both settle `Interrupted`; only the appended note names the real cause | 1.9 spec §8, deliberate | ☐ open **by decision**. Needs a new `DoneStatus` variant, which is a `PROTOCOL_VERSION` question — bundle it into a bump made for another reason rather than spending one here |
| [D27](D27-sweeper-call-site-uncovered.md) | `Engine::assemble_runtime`'s sweeper spawn (`crates/engine/src/lib.rs:389-393`) is exercised by no test: **deleting it, or swapping `bound` for `sweep_interval(bound)` in it, would leave the whole suite green**. The existing sweeper test calls `spawn_unattended_sweeper` itself and so bypasses the call site | 1.9 Task 5 review, controller ruling: do not reopen | ☐ open. A timing-sensitive test would flake; the page names a type-safety fix for the transposition half and what the deletion half would need |
| D28 | Recovery from an expired turn is manual: the note tells the user to send again, and there is no button that does it — the prompt sits in the transcript with nothing wired to resubmit it | 1.9 spec §8, deliberate | ☐ open **by decision**. Same shape as D14's residual (a note the composer promises but the UI doesn't finish); candidate to close alongside it |
| [D29](D29-administrative-clients-count-as-supervisors.md) | **An administrative CLI call counts as a supervisor.** `comet status` and the `comet remote …` subcommands open the same IPC websocket the Desktop app does, so each one clears `unattended_since` and stamps a fresh stretch on exit — a cron polling `comet status` every five minutes means a parked approval **never expires**, with no log line saying why | 1.9 whole-branch review | ☐ open **by decision**. Fails in the safe direction; distinguishing a supervising viewport from a one-shot administrative call needs a hello-handshake field and a `PROTOCOL_VERSION` bump |
| D30 | **A forced discovery retry can swallow another caller's drift signal.** `DiscoveryCache` reports an unreadable answer once per attempt, and `clear()` starts a fresh attempt — so a forced retry landing between another caller's `get()` and its report swaps the attempt out, and that caller finds nothing to report. One `Unparseable` then raises no `Diagnostic` | 2.1 PR review (#41) | ☐ open **by decision**. Only loses the signal when the retry it raced *succeeded*, so the drift is over by then. Closing it means returning the failure alongside the catalog from `Harness::models()`, changing the trait's return type and every fixture implementing it, for a window that needs a concurrent forced retry. Documented on `take_unreported_failure` |
| D31 | The initialize handshake returns `agents` on the same reply comet already reads for models, and it is deliberately dropped. **After 4.2 the subagent *events* exist** (`SubagentStarted`/`SubagentUpdated`, tasks 1–7) and the list is still unconsumed, for a different reason: `agentType` arrives per-run on `task_started` — `agent.rs:1425-1427`'s own comment says it is "never looked up in the discovery handshake's `agents` catalogue" — so nothing needs the catalogue, and the entries' `description` fields are long model-facing prose (the `Explore` entry measures **605 characters** of instructions in the 2026-08-11 initialize capture), which is not card copy | 2.2, deliberate (spec departure 1); reason corrected by 4.2 | ☐ open **by decision** — free data, still no consumer, and no later slice now owns closing it (4.2 was the named owner and reconfirmed the row rather than closing it). Wire shape is in the 2026-08-11 initialize capture |
| D53 | **The subagent surface itself is not built.** The events and the persisted part land (tasks 1–7 of 4.2) but `transcript.rs`'s render loop has an explicit `MessagePart::Subagent { .. } => {}` no-op arm (`crates/ui/src/transcript.rs:608-614`) — persisted, never drawn, by the arm's own comment | 4.2, deliberate — no UI ships this slice | ☐ open **by decision**. Owner is named in code: `transcript.rs:608-614`'s comment says rendering it as a card is **slice 4.4's** build. The arm is explicit, not a wildcard, so a future `MessagePart` kind added beside `Subagent` still fails to compile until handled. **For 4.4:** a terminal card keeps its live `activity` line — `doc/src/parts.rs`'s `SubagentUpdated` fold only overwrites `activity` when the new reading reports one (`Some`), so a `task_updated`/`task_notification` that carries no `activity` field leaves the last live line standing after the card goes terminal. 4.4 must not draw that stale live-activity line on a finished card |
| D54 | **The child subagent's own transcript is not reachable from the parent's card** — only its activity line, summary and totals. `AgentEvent::SubagentStarted`'s `tool_use_id` is kept for exactly this and matched nowhere as a key today | 4.2, deliberate | ☐ open — 4.4's spec is unwritten and nothing claims it yet. `crates/doc/src/parts.rs:356-357`'s comment: "`tool_use_id` is kept on the event for the journal and a later slice's child-transcript render, never as a match key here" |
| D55 | **`workflow_progress` — a coordinator's `phases[]`/`agents[]` array on `task_progress` — is never decoded.** A single `Agent` call never produces it, so 4.2's capture has none. **Not decoding it is what keeps one coordinator tick at one `SubagentUpdated`** — t3code fans the array out into per-member events, which is where the up-to-100-events-per-tick risk lives; Comet never reading the array is what avoids it, not the dedupe filter. The material-transition filter (`SubagentSnapshot`, `normalize.rs:140-157`, reached via `emit_subagent_update` at `:464-491`) is an orthogonal mechanism Task 3 built to collapse repeated *identical* `task_progress` ticks — it would do nothing against a real fan-out of distinct per-member frames | 4.2 spec risk, decided in Task 3 | ☐ open **by decision**, deferred at spec time rather than found mid-slice. The first user to run a `Workflow` produces the first real frame; there is still no capture of one. **If a future slice decodes the array, it must build its own fan-out guard — the existing filter does not provide one** |
| D56 | **`render_parts`'s catch-all passes a `Subagent` part's `summary` and `description` through unfiltered, and `summary` is the child's full answer, uncapped end-to-end.** Its own `Approval` arm comment (`crates/engine/src/sessions.rs:1346-1351`) says a kind carrying something heavy "has to make that decision here rather than inherit it silently" — `Subagent` inherits it silently. `prompt` genuinely has nothing to strip: the persisted struct (`crates/doc/src/parts.rs:95-118`) has no `prompt` field at all. `summary` is different — `normalize.rs:553` assigns it straight from the wire (`summary: f.summary`) with no `cap_prose`, unlike `prompt` (`:511-513`) and `Notice` text (`:367`, `:385`); the fold (`doc/src/parts.rs:456`) adds no cap either, so the child's unbounded answer reaches the persisted doc and every LAN peer's copy | 4.2 review; `summary`'s cap decided in Task 3's own review round (Finding 3) | ☐ open, low priority. **The `summary` half is a decision, not a gap.** Task 3's review round weighed capping it against `TextDelta` and `Done.result` — also uncapped anywhere in `normalize.rs`, also the product's own displayed content — and concluded a subagent's answer is that same kind of content, not `Notice`/`prompt`'s kind (deliberately kept out of what's shown), so capping it here would truncate real content before any surface decides how to present it. `description` crosses the same catch-all with no dedicated cap either, unreviewed — it has only ever been observed as short task-title text, never reasoned about the way `summary` was. What is missing is that `render_parts`'s own site — the one whose comment demands the decision be made there — records none of this |
| [D57](D57-steer-leaves-a-subagent-running-forever.md) | **Task 7's "no `Subagent` left `Running`" guarantee is not total.** A steer finishes the pre-steer segment `Complete` (`sessions.rs:1774`) and deliberately leaves a still-`Running` `Subagent` part untouched (`cancel_running_subagents`'s doc, `sessions.rs:1440-1461`); `DocHost::mark_abandoned_streams` sweeps only `Streaming` entries (`doc_host.rs:163`), so the crash-path sweep task 7 added doesn't reach a finished segment either — a subagent still running when the user steers stays `Running` forever in that segment | 4.2, deliberate (user ruling) | ☐ open **by decision**, documented at the source and on the linked page. `sessions.rs:1440-1461` is the record: a steer never touches the child process, so stamping `Cancelled` there would assert an outcome nobody observed, and a stale `Running` reading beats a manufactured one. Proven end-to-end by `crates/engine/tests/e2e.rs:2614-2675` |
| D32 | The slash-command cache the phase spec puts in 2.2 is not built: `commands` is **cwd-scoped** (66 entries in this repo vs 63 in a home directory, the delta being its project skills) while `Harness::models(&self)` has no cwd to key on | 2.2 capture, 2026-08-11 | ☐ open **by decision** — moved to **2.4**, which owns the `/` menu and has a cwd in hand. Reading it here would cache another directory's skills. **2.4 also cannot reuse the discovery spawn**: it passes `--bare`, which skips user and project skill discovery (42 commands vs 66), so the command list needs a spawn of its own |
| D33 | `claude-opus-4-8` and `claude-opus-4-7` are curated but absent from Claude's live model list, so nothing live confirms them and no test would notice if they were retired | 2.2 capture, 2026-08-11 | ☐ open, low priority. **They still work — they are simply not in the official list** (user, 2026-08-11), and the handshake cannot tell you: the CLI does not validate `--model` at session init, where a bogus id also answered and exited 0. Merge rule 1 (discovery never deletes a curated model) is what keeps them, and a retired model fails loudly at run time |
| D34 | **The run path has the relative-executable hazard 2.2 just fixed in the discovery path.** `build_command` sets `current_dir(&request.cwd)` while passing `exe` through unchanged, and `resolve_cli` returns `CLAUDE_CODE_EXECUTABLE` verbatim (`lib.rs:161-163`) — so a relative override plus a session cwd resolves the program against the wrong directory on Unix, per std's documented `current_dir` caveat | 2.2 PR review (#43), adjacent | ☐ open, pre-existing and untouched by 2.2. The fix is `claude/discovery.rs`'s `program_path`, applied in `build_command` and in Codex's spawn; needs checking whether a failed run spawn surfaces better than discovery's silent fallback |
| D35 | **Codex reports per-model deprecation copy and Comet drops it.** `model/list` carries `upgrade` (a replacement model id) and `upgradeInfo.migrationMarkdown` ("GPT-5.4 will be deprecated soon… Switch to GPT-5.6 Terra to continue.") on `gpt-5.4` and `gpt-5.4-mini`; 2.3 models neither | 2.3 capture, 2026-08-11 | ☐ open, low priority. Phase 0b's "neither provider emits a deprecation notice" was about the **event stream** and is still true — this is a field on a catalog reply. A notice surface already exists (`AgentEvent::Notice`, 0b.1); the open question is whether a catalog reply is a legitimate producer for it |
| D36 | **`defaultReasoningEffort` has nowhere to go.** Codex reports a different default effort per model (`sol` low, `terra`/`luna` medium, `codex-spark` high) and `comet_proto::Model` carries a ladder with no declared default, so the picker cannot show or honour it | 2.3 capture, 2026-08-11 | ☐ open. Adding it is a `Model` field, so it costs a `PROTOCOL_VERSION` question — read that constant's doc comment before deciding whether an absent value is safe for an older peer |
| D37 | **Two models are offered a service tier the server does not list for them.** `gpt-5.4-mini` and `gpt-5.3-codex-spark` come back with `serviceTiers: []`, while `codex/catalog.rs` gives all seven curated models the same `serviceTier` option — and merge rule 2 (curated capability wins on a matched id) preserves that by design | 2.3 capture, 2026-08-11 | ☐ open, low priority. Pre-existing; 2.3 deliberately did not touch the merge rule to fix it. Closing it means letting a live answer *remove* a curated option, which is the opposite of rule 2 and needs a ruling, not a patch |
| D38 | **A logged-out `codex` answers `model/list` successfully with a five-model list baked into the binary** — it contains `gpt-5.2`, which the account cannot use, and omits three models it has. Comet's defence is a pre-spawn `auth.json` check, which cannot see a user authenticated purely by an `OPENAI_API_KEY` environment variable | 2.3 capture run 6, 2026-08-11 | ☐ open **by decision**. Fails safe: an env-only user gets the curated list under the "couldn't reach Codex" caption, never a list belonging to nobody. Closing it properly means asking the server who is logged in (`account/*`), which needs its own capture |
| D42 | **The composer's resting height changes with the space.** `Pickers::render_footer` returns `None` unless a space row exists *and* `space.git_detected` (`pickers.rs:1999-2007`), so a git space gets a branch/worktree toolbar under the pill and a non-git one does not — about **36px** of difference (20px chips, 8px above from the container gap, 8px below carved out of its bottom padding with `mb(-8)`). Added by `47f8ba6` (2026-07-23, the t3code BranchToolbar port), which predates this repo's numbered PRs — the first is #2 on 2026-07-26 | noticed 2026-08-11 while checking whether slice 2.4 had changed composer geometry; it had not | ☐ open. **The height itself is wanted** (user, 2026-08-11); the defect is that it is conditional, so the composer sits at two different heights depending on where you are. `Theme::STATUS_STRIP_HEIGHT` is the precedent for the fix — that strip is reserved whether or not it has content, precisely so the composer never shifts. Look at whether the same reservation suits a toolbar whose *contents* are meaningless outside a git space |
| [D39](D39-codex-skill-input-item.md) | **Codex has a real command surface and 2.4 does not use it.** Its app-server does not parse slash commands at all — `/name` reaches the model as literal text and spends a turn on a refusal — but `skills/list` answers in 0.36s with no thread, and `turn/start` accepts a structured `{type:"skill", name, path}` input item, verified working | 2.4 capture, 2026-08-11 | ☐ open **by decision** (user ruling, 2026-08-11). Needs a structured reference to survive composer draft → proto → engine → harness, which is a wire design with a second consumer: Codex's `{type:"mention"}` item for `@` files, which Comet also does not use. Its own slice |
| D40 | **The `/` menu's spawn runs the user's `SessionStart` hooks.** It is the non-bare handshake by necessity (`--bare` drops user and project skills, 42 against 67), so every first `/` in a new directory runs whatever the user has configured — 3.5s of hook in the 2.2 measurement, and arbitrary side effects in principle | 2.4, deliberate | ☐ open **by decision**. Bounded by the per-directory per-boot cache, so it is once per directory, not once per keystroke. Closing it properly needs a CLI flag that skips hooks without skipping skills, which does not exist today |
| D50 | **A second install of the same CLI is invisible, and PATH order alone decides which one runs.** The capture machine has Codex twice — native `0.147.0` under `%LOCALAPPDATA%\Programs\OpenAI\Codex\bin` and npm-global `0.146.0` under `%APPDATA%\npm` — and `npm ls -g` confirms the npm one. `resolve_cli` (`harness/src/lib.rs:171-200`) flat-maps *directories* first and filename spellings second, so directory order dominates and `executable_names`' `.exe`-before-`.cmd` rule only breaks ties **inside one directory**. Reorder PATH and Comet silently runs the older CLI | 3.1 capture, 2026-08-11 (`captures/2026-08-11-agent-version-install-method.md`) | ☐ open **by decision** (user ruling, 2026-08-11). 3.1 shows the resolved install only. Detecting a shadowed one means continuing the directory scan past the first hit and spawning `--version` per extra candidate at every boot, plus a new user-facing state with its own copy and failure story. The version+path line 3.1 adds is what makes the situation *diagnosable* today: the path names which install answered. Revisit with 3.2 — `claude install` is the native updater and cannot update an npm-global install, so an Update button has to know |
| D51 | **A store-level identity failure paints a raw technical string on the Retry card**: `DACL: identity I/O: C:\...\device-identity.pem does not have the private...`, truncated mid-sentence. Reproduced by copying a `COMET_DATA_DIR` to a new path, which breaks the key file's restrictive ACL. Rule 1 of `.agents/rules/user-facing-errors.md` — no raw error on screen — with no `summary`/`hint` split and no action beyond Retry, which cannot succeed | 4.1 rendered check, 2026-08-12 | ☐ open. Same family as **D4**, but a different site: this is a *startup* path, not one of the thirteen mutation sites that inventory covers |
| [D52](D52-ui-state-previews.md) | **There is no way to see a UI state without producing it for real.** Reviewing the 4.1 gauge at four fills needed a filled context window; the ad-hoc escape was a one-off `COMET_CONTEXT_DEMO` env knob, and the attempt before it spent three real Fable turns driving the app into the state | 4.1 rendered check, 2026-08-12 | ☐ open, **unowned**. Every rendered check since 0.2a has re-invented this per surface; the page lists the options and what each costs |
| D41 | **The command cache never refreshes inside a boot.** A skill added to the repo while Comet is open is invisible to the `/` menu until restart, and an unrecognized command fails *silently-successfully* — the CLI answers `Unknown command: /x` as ordinary assistant text with `is_error: false` and no model call, so nothing downstream can detect the staleness either | 2.4, deliberate | ☐ open. The picker's Retry already clears it (`clear_discovery` clears both caches); what is missing is a trigger the user would think to reach for. Codex publishes a `skills/changed` notification for exactly this, which is a reason to solve it alongside D39 rather than alone |
| [D43](D43-fake-provider-launch-contract.md) | **The fake providers do not prove the full process launch contract.** Claude ignores every run argument except permission mode; Codex never checks the `app-server` subcommand; resume, attachment and actual child-cwd paths remain partly or wholly outside subprocess coverage | fake-provider review, 2026-08-11 | ☐ open, highest-leverage first hardening step. D34 separately owns relative executable resolution |
| [D44](D44-provider-capture-corpus.md) | **The provider capture corpus is partial.** Every retained capture-derived claim has sanitized, versioned literal evidence, including the successful Codex steer request/reply/completion sequence. The all-twelve-scenarios criterion remains unmet: ordinary approval, on-request approval, and interruption produced only quarantined contradictions | fake-provider review, 2026-08-11; bounded capture work, 2026-08-12 | ◐ partly closed. The three ignored partials cannot satisfy claims; checking incomplete evidence into the corpus requires a separate contradiction-artifact design |
| [D45](D45-provider-lifecycle-fault-matrix.md) | **The provider fakes cannot express most lifecycle faults.** There is no setup-stage hang, crash-plus-stderr, partial frame, broken stdin, late reply, or provider tool call that never completes | fake-provider review, 2026-08-11 | ☐ open, preferred reliability slice. D5 covers the user-visible hung-call state, not a provider-fake reproducer |
| [D46](D46-provider-process-tree-cleanup.md) | **Cancellation tests prove the direct fake exits, not that a provider-owned process tree is gone.** The fixtures spawn no descendants, while both platform helpers signal or terminate only the direct pid | fake-provider review, 2026-08-11 | ☐ open across shipped platforms; Windows adds behaviour the Ubuntu PR gate cannot exercise |
| [D47](D47-live-provider-canaries.md) | **No automated live turn checks the scripted run protocols for drift.** The ignored real-CLI tests cover token-free discovery only; approvals, steering, notices, interruption and terminal mapping rely entirely on fakes and historical captures | fake-provider review, 2026-08-11 | ☐ open by environment constraint. Candidate for an opt-in or scheduled authenticated canary, never the ordinary PR gate |
| [D48](D48-provider-state-sequences.md) | **Named scenarios cover examples, not the event-order state space.** JSON-RPC response/notification races and stream lifecycle permutations can regress outside the hand-written transcripts | fake-provider review, 2026-08-11 | ☐ open, after D45 supplies reusable fault primitives. D8–D10 already own the known diagnostic-policy gaps |
| [D49](D49-provider-cross-boundary-tests.md) | **The provider subprocess fakes stop at the harness boundary.** Engine tests use in-process harness doubles, so selected launch, approval, discovery and terminal-state paths never cross fake executable → harness → engine → RPC in one test | fake-provider review, 2026-08-11 | ☐ open, targeted integration coverage rather than a duplicate full suite |
| D58 | **`comet-provider-sanitize` has no rule for a subagent `task_id`, in any context, and none for `task_notification`'s `output_file` field at all.** `normalize_field("task_id")` is `"taskid"`, which matches none of `semantic_kind`'s recognized key spellings, so it is never typed — and `output_file` embeds the operator's absolute cwd as the same hyphen-mangled slug `memory_paths.auto` uses, plus a raw copy of `session_id` and `task_id`, none of which get redacted because there is no value-based sweep that replaces an already-typed identifier when it turns up embedded, unkeyed, inside a different field | found hand-sanitizing `claude/2.1.229/subagent` for 4.2 task 9; pinned by `sanitizer_has_no_rule_for_task_id_or_output_file_mangled_cwd_slug_known_gap` (`crates/harness/tests/capture_corpus/sanitizer_semantics.rs`) | ☐ open. The fix is two `semantic_kind` arms (a `"taskid"` case alongside `"tooluseid"`, and an `"outputfile"` case that runs the existing path/id substitution rather than a fresh literal match) — small in isolation, but `output_file`'s value needs the *same* mangled-slug decoder `claude_memory_paths` already has, which is worth sharing rather than duplicating. Deferred rather than fixed in 4.2 task 9, which is a verify task, not a `sanitize.rs` change |
| D59 | **A terminal `Subagent` card can be reopened by a status the terminal guard doesn't recognize as terminal — one route closed this dispatch, a sibling route left open.** Route A: `task_updated`/`task_notification`/the `tool_use_result` fallback carrying a status string `subagent_status` doesn't recognize (a future CLI spelling — "succeeded", "timed_out", …) used to degrade unconditionally to `Running` even after a stored terminal reading, permanently reopening a finished card; fixed this dispatch in `status_or_carry_forward` (`crates/harness/src/claude/normalize.rs`), which now carries the stored terminal status forward instead. Route B, NOT fixed: `task_progress` (`normalize_subagent_task`'s `"task_progress"` arm) assigns `status: SubagentStatus::Running` UNCONDITIONALLY, with no read of any prior state — a `task_progress` tick arriving after a terminal reading produces a non-terminal snapshot, so `emit_subagent_update`'s terminal-vs-terminal guard (which requires BOTH sides terminal to suppress) never applies, and the card reopens as `Running` the same way Route A used to | 4.2 fix-wave review | ☐ open for Route B. Not observed live — the capture never showed a `task_progress` after a terminal reading — but nothing on the wire rules it out, and the mechanism is identical to Route A's now-fixed bug. Closing it means `task_progress` reading prior state the same way `status_or_carry_forward` now does, rather than hardcoding `Running` |

## Closed

| # | Item | Origin | State |
| --- | --- | --- | --- |
| [D1](closed.md) | Empty `ReasoningDelta` while parked flips the session to Working and permanently disarms the 30-min idle reaper | 0b.1 whole-branch review | ☑ merged [PR #27](https://github.com/matty/comet/pull/27) |
| [D2](closed.md) | "Write the `None` case yourself" — optional wire fields need their absent case written by hand; a plan's fixtures will not generate it | 0b.1 Greptile P1 | ☑ merged [PR #28](https://github.com/matty/comet/pull/28) → `.agents/rules/optional-wire-fields.md` |
| [D5](D05-hung-tool-call.md) | A Codex tool call can hang **forever** with no timeout and no recovery — observed live, 12+ minutes on one exec call, session pinned Working | 0b.2 real-CLI check | ☑ merged [PR #36](https://github.com/matty/comet/pull/36) (`5a8a16e`). **Read the page's "what closed means" note before treating it as gone** |
| [D6](closed.md) | Claude's unclaimed `control_request` subtypes are counted but never answered; `sdk.d.ts` says hosts must reply `{behavior:"cancelled"}` to unrecognized dialog kinds | 0b.2 sink 3 | ☑ closed by 1.6 ([PR #37](https://github.com/matty/comet/pull/37), `61fec51`). Written **blind** — nine capture runs produced zero non-`can_use_tool` control requests. The diagnostic was kept: answering a request does not make it understood |
| [D11](D11-remembered-request-mode.md) | A run's remembered `last_request` carries the **previous** turn's runtime mode; three paths preferred it over the chat row, so a mid-chat mode change would not reach a steered or resumed run | 1.1 whole-branch review | ☑ closed by 1.8 ([PR #39](https://github.com/matty/comet/pull/39)). `DocHost::apply_chat_row_runtime_mode` overlays the row's current mode on all three paths and re-derives the sandbox from it. **The ruling the page records: a mode change applies to the next dispatch, never to a run in flight** — the provider process was spawned with its permission mode |
| [D12](D12-permission-axis-version.md) | The permission axis crosses RPC with no version signal: two same-`PROTOCOL_VERSION` builds can pair, and the older one silently runs a request under the default mode | 1.2 Greptile review, premise verified | ☑ closed by 1.8 ([PR #39](https://github.com/matty/comet/pull/39)). `PROTOCOL_VERSION` is `4`, so a mixed pair is refused outright rather than running a session under a mode nobody chose. Note this is **not** the usual decode-driven bump: the field was always additive, but its absence is indistinguishable from a deliberate value |
| D24 | **The deny note never reaches Codex, and the field said it would.** Comet persists it, but Codex's decision literals have no message field, so the wire carries `"decline"` and nothing else | 1.7 live check, scenario 3 | ☑ closed by 1.8 ([PR #39](https://github.com/matty/comet/pull/39)). `HarnessCapabilities::carries_deny_note` (conservative default `false`) drives the field's label, so a provider that cannot deliver the note no longer has a field promising it. **The note is still undeliverable on Codex — nothing invented a channel.** D14's residual is the mirror image and stays open |
