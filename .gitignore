/target
.worktrees/
.comet-provider-captures/
**/*.rs.bk
*.pdb
.DS_Store
.env
.env.local
node_modules
.wrangler
build/
edge/dist/
**/__pycache__/

# Leaked by 	empfile::tempdir() when TMPDIR resolves inside the checkout —
# the codex suite's logged-in CODEX_HOME fixture creates one per run.
.tmp*/
